import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs';
const matrix=JSON.parse(fs.readFileSync(new URL('../benchmarks/bakeoff/mitosis/matrix.json',import.meta.url)));
test('mitosis matrix has complete bound identities and non-optimistic statuses',()=>{assert.equal(matrix.rows.length,28);const allowed=new Set(['passed','failed','not-run','blocked']);for(const r of matrix.rows){assert.equal(r.candidate,'mitosis');assert.ok(r.component&&r.framework&&r.kumoRevision&&r.candidateRevision&&r.runId);for(const value of Object.values(r.evidence)){assert.equal(typeof value,'string');assert.ok(allowed.has(value));}}const actual={passed:0,failed:0,'not-run':0,blocked:0};for(const r of matrix.rows)for(const v of Object.values(r.evidence))actual[v]++;assert.deepEqual(matrix.counts,actual);assert.equal(matrix.adaptationFrequency,28);});
