import test from'node:test';import assert from'node:assert/strict';import{validateFixturePlan,fixturePlanDigest}from'../../proof/dx/conformance/shared/fixture-plan.mjs';
const plan={schemaVersion:'kumo.fixture-plan/v1',component:'button',vector:'enabled-click',contract:{schemaVersion:'kumo.observable/v1',path:'contracts/kumo.observable/v1/components/button.json',sha256:'a'.repeat(64)},fixture:{kind:'component',children:[{kind:'text'}]},setup:[{kind:'event-recorder'}],actions:[{type:'click'}],probes:[{kind:'node-identity'},{kind:'events'}],assertions:[{kind:'identity'},{kind:'sequence'}],capabilities:['native-button'],bundle:{entry:'client.js',viteConfig:'vite.config.mjs',files:{'App.js':'export default 1'},headHtml:'',beforeAppHtml:'',buildEnv:{MODE:'test'}}};
test('fixture plan validates exact neutral operations deterministically',()=>{assert.equal(validateFixturePlan(structuredClone(plan)).vector,'enabled-click');assert.equal(fixturePlanDigest(plan),fixturePlanDigest(JSON.parse(JSON.stringify(plan))))});
test('fixture plan rejects unsupported operations and unsafe files',()=>{for(const mutate of[p=>p.actions.push({type:'dispatch-event'}),p=>p.probes.push({kind:'secret'}),p=>p.assertions.push({kind:'skip'}),p=>p.bundle.files['../escape.js']='x',p=>p.extra=true]){const candidate=structuredClone(plan);mutate(candidate);assert.throws(()=>validateFixturePlan(candidate),/invalid fixture plan/)}});
