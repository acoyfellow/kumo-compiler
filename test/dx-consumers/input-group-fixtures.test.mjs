import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';import crypto from'node:crypto';import{fixturePlanDigest,validateFixturePlan}from'../../proof/dx/conformance/shared/fixture-plan.mjs';import{executeInputGroupPlan}from'../../proof/dx/conformance/shared/input-group-executor.mjs';
const path='proof/dx/conformance/shared/input-group-fixtures.json',disk=JSON.parse(fs.readFileSync(path));const canonical=value=>Array.isArray(value)?`[${value.map(canonical).join(',')}]`:value&&typeof value==='object'?`{${Object.keys(value).filter(k=>value[k]!==undefined).sort().map(key=>`${JSON.stringify(key)}:${canonical(value[key])}`).join(',')}}`:JSON.stringify(value);const digest=value=>crypto.createHash('sha256').update(canonical(value)).digest('hex');
test('three input-group plans preserve packed canonical provenance and expectations',()=>{assert.deepEqual(disk.plans.map(p=>`input-group/${p.vector}`),['input-group/composition','input-group/field-focus','input-group/disabled-error']);const sourceText=fs.readFileSync('contracts/kumo.observable/v1/components/input-group.json','utf8'),source=JSON.parse(sourceText),packed=JSON.parse(fs.readFileSync('proof/dx/conformance/shared/packed-fixtures.json'));for(const plan of disk.plans){validateFixturePlan(plan);assert.equal(plan.digest,fixturePlanDigest({...plan,digest:undefined}));assert.equal(plan.contract.sha256,crypto.createHash('sha256').update(sourceText).digest('hex'));assert.equal(plan.contract.vectorSha256,digest(source.vectors.find(v=>v.id===plan.vector)));const packedVector=packed.vectors.find(v=>v.provenance.vector===`input-group/${plan.vector}`);assert.equal(plan.contract.sha256,packedVector.provenance.contractDigest);assert.deepEqual(plan.fixture,packedVector.fixture);assert.deepEqual(plan.actions,packedVector.actions.map(({trustedRunner,...action})=>action));const expected=Object.fromEntries(plan.assertions.filter(a=>a.probe!=='node-identity').map(a=>[a.probe==='dom'?'root':a.probe,a.expected]));assert.deepEqual(expected,packedVector.expected);assert.ok(plan.probes.some(p=>p.kind==='node-identity'))}assert.equal(disk.digest,fixturePlanDigest({...disk,digest:undefined}))});
test('pure input-group executor delegates trusted operations deterministically',async()=>{for(const plan of disk.plans){const calls=[];const makeRunner=()=>{let value='',focus='none';return{setup:async x=>calls.push(['setup',x]),action:async x=>{calls.push(['action',x]);if(x.type==='click'&&x.selector==='label')focus='input';if(x.type==='type'){value+=x.text;focus='input'}if(x.type==='focus')focus=x.selector},probe:async x=>{calls.push(['probe',x]);if(x.kind==='dom')return{tag:'div',extra:true};if(x.kind==='state')return{values:[value]};if(x.kind==='focus')return focus;return'preserved'}}};const first=await executeInputGroupPlan(makeRunner(),plan),second=await executeInputGroupPlan(makeRunner(),plan);assert.deepEqual(first,second);assert.deepEqual(first.probes.dom,{tag:'div',extra:true});assert.doesNotMatch(JSON.stringify(calls),/dispatchEvent|new Event|synthetic|lifecycle/i);await assert.rejects(()=>executeInputGroupPlan({...makeRunner(),browser(){}},plan),/unsupported runner field/)}});
test('input-group executor fails closed for unsupported probes',async()=>{const plan=structuredClone(disk.plans[0]);plan.probes[0].kind='events';plan.digest=undefined;const runner={setup:async()=>{},action:async()=>{},probe:async()=>null};await assert.rejects(()=>executeInputGroupPlan(runner,plan),/unsupported input-group probe/)});
