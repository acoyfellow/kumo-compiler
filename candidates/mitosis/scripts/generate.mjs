import fs from 'node:fs'; import { parseJsx, componentToReact, componentToVue, componentToSvelte, componentToSolid } from '@builder.io/mitosis';
const names=['Button','Field','Tabs','Select','Dialog','Popover','DatePicker']; const targets={react:componentToReact({typescript:true}),vue:componentToVue({typescript:true}),svelte:componentToSvelte({typescript:true}),solid:componentToSolid({typescript:true})};
for(const name of names){const source=`import { useStore } from '@builder.io/mitosis'; export default function ${name}(props) { const state = useStore({ active: false }); return <div data-component="${name}" aria-label={props.label}>{props.children}</div>; }`; const model=parseJsx(source,{typescript:true}); for(const [target,gen] of Object.entries(targets)){const dir=new URL(`../generated/${target}/`,import.meta.url);fs.mkdirSync(dir,{recursive:true});try{fs.writeFileSync(new URL(`${name}.${target==='vue'?'vue':target==='svelte'?'svelte':'tsx'}`,dir),gen(model));}catch(e){fs.writeFileSync(new URL(`${name}.blocked.txt`,dir),String(e));}} }
