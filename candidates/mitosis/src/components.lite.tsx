import { useStore } from '@builder.io/mitosis';
export function Button(props: { children?: any; disabled?: boolean; onClick?: () => void }) { return <button type="button" disabled={props.disabled} onClick={() => props.onClick?.()}>{props.children}</button>; }
export function Field(props: { label: string; value?: string; defaultValue?: string; onInput?: (value:string)=>void }) { return <label>{props.label}<input value={props.value} defaultValue={props.defaultValue} onInput={(e:any)=>props.onInput?.(e.target.value)} /></label>; }
export function Tabs(props: { labels: string[]; value?: number; defaultValue?: number; onChange?: (v:number)=>void }) { const state=useStore({active: props.defaultValue || 0}); return <div><div role="tablist">{props.labels.map((label,i)=><button role="tab" aria-selected={(props.value ?? state.active)===i} onClick={()=>{state.active=i;props.onChange?.(i)}}>{label}</button>)}</div><div role="tabpanel">{props.labels[props.value ?? state.active]}</div></div>; }
export function Select(props: { label:string; options:string[]; value?:string; onChange?:(v:string)=>void }) { return <label>{props.label}<select value={props.value} onChange={(e:any)=>props.onChange?.(e.target.value)}>{props.options.map(x=><option value={x}>{x}</option>)}</select></label>; }
export function Dialog(props: { open:boolean; title:string; children?:any; onClose?:()=>void }) { return <div role="dialog" aria-modal="true" aria-label={props.title} hidden={!props.open}>{props.children}<button onClick={()=>props.onClose?.()}>Close</button></div>; }
export function Popover(props: { label:string; open?:boolean; children?:any }) { const state=useStore({open:false}); return <div><button aria-expanded={props.open ?? state.open} onClick={()=>state.open=!state.open}>{props.label}</button><div hidden={!(props.open ?? state.open)}>{props.children}</div></div>; }
export function DatePicker(props: { label:string; value?:string; onChange?:(v:string)=>void }) { return <label>{props.label}<input type="date" value={props.value} onInput={(e:any)=>props.onChange?.(e.target.value)} /></label>; }
