import{readFile,writeFile}from'node:fs/promises';
const spec=JSON.parse(await readFile(new URL('../specs/popover.json',import.meta.url)));const cases=JSON.stringify(spec.states);const css=`import'../../../../public/popover.css';`;
const react=`import React,{useEffect,useRef,useState}from'react';import{hydrateRoot}from'react-dom/client';${css}const cases=${cases};function Popover(p){const[open,setOpen]=useState(!!p.open),root=useRef(),id='popover-'+p.label.toLowerCase().replace(/\\W/g,'-');useEffect(()=>{const outside=e=>{if(p.closeOnOutside!==false&&!root.current.contains(e.target))setOpen(false)},key=e=>{if(e.key==='Escape'&&p.closeOnEscape!==false){setOpen(false);root.current.querySelector('.popover-trigger')?.focus()}};document.addEventListener('pointerdown',outside);document.addEventListener('keydown',key);return()=>{document.removeEventListener('pointerdown',outside);document.removeEventListener('keydown',key)}},[]);return <section ref={root} className="popover-demo"><strong>{p.label}</strong><button className="popover-trigger" aria-haspopup="dialog" aria-expanded={open} aria-controls={id} onClick={()=>setOpen(v=>!v)}>Toggle</button>{open&&<div id={id} role="dialog" aria-label={p.label} className={'kumo-popover side-'+p.side+' align-'+p.align} style={{'--side-offset':(p.sideOffset??8)+'px','--align-offset':(p.alignOffset??0)+'px'}}><p>Popover content</p><button onClick={()=>setOpen(false)}>Close</button></div>}</section>}function App(){return <main className="popover-shell"><h1>Popover</h1><div className="popover-matrix">{cases.map(p=><Popover key={p.label}{...p}/>)}</div></main>}hydrateRoot(document.getElementById('app'),<App/>);`;
const vue=`<script setup>import{ref,onMounted,onUnmounted}from'vue';const p=defineProps(['label','side','align','sideOffset','alignOffset','open','closeOnOutside','closeOnEscape']);const shown=ref(!!p.open),root=ref(),trigger=ref(),id='popover-'+p.label.toLowerCase().replace(/\\W/g,'-');const outside=e=>{if(p.closeOnOutside!==false&&!root.value.contains(e.target))shown.value=false},key=e=>{if(e.key==='Escape'&&p.closeOnEscape!==false){shown.value=false;trigger.value.focus()}};onMounted(()=>{document.addEventListener('pointerdown',outside);document.addEventListener('keydown',key)});onUnmounted(()=>{document.removeEventListener('pointerdown',outside);document.removeEventListener('keydown',key)})</script><template><section ref="root" class="popover-demo"><strong>{{p.label}}</strong><button ref="trigger" class="popover-trigger" aria-haspopup="dialog" :aria-expanded="shown" :aria-controls="id" @click="shown=!shown">Toggle</button><div v-if="shown" :id="id" role="dialog" :aria-label="p.label" :class="['kumo-popover','side-'+p.side,'align-'+p.align]" :style="{'--side-offset':(p.sideOffset??8)+'px','--align-offset':(p.alignOffset??0)+'px'}"><p>Popover content</p><button @click="shown=false">Close</button></div></section></template>`;
const svelte=`<script>import{onMount}from'svelte';export let label,side,align,sideOffset=8,alignOffset=0,open=false,closeOnOutside=true,closeOnEscape=true;let shown=open,root,trigger;const id='popover-'+label.toLowerCase().replace(/\\W/g,'-');onMount(()=>{const outside=e=>{if(closeOnOutside&&!root.contains(e.target))shown=false},key=e=>{if(e.key==='Escape'&&closeOnEscape){shown=false;trigger.focus()}};document.addEventListener('pointerdown',outside);document.addEventListener('keydown',key);return()=>{document.removeEventListener('pointerdown',outside);document.removeEventListener('keydown',key)}})</script><section bind:this={root} class="popover-demo"><strong>{label}</strong><button bind:this={trigger} class="popover-trigger" aria-haspopup="dialog" aria-expanded={shown} aria-controls={id} onclick={()=>shown=!shown}>Toggle</button>{#if shown}<div {id} role="dialog" aria-label={label} class="kumo-popover side-{side} align-{align}" style="--side-offset:{sideOffset}px;--align-offset:{alignOffset}px"><p>Popover content</p><button onclick={()=>shown=false}>Close</button></div>{/if}</section>`;
const solid=`import{createSignal,onCleanup,onMount}from'solid-js';export default function Popover(p){const[shown,setShown]=createSignal(!!p.open);let root,trigger;const id='popover-'+p.label.toLowerCase().replace(/\\W/g,'-');onMount(()=>{const outside=e=>{if(p.closeOnOutside!==false&&!root.contains(e.target))setShown(false)},key=e=>{if(e.key==='Escape'&&p.closeOnEscape!==false){setShown(false);trigger.focus()}};document.addEventListener('pointerdown',outside);document.addEventListener('keydown',key);onCleanup(()=>{document.removeEventListener('pointerdown',outside);document.removeEventListener('keydown',key)})});return <section ref={root} class="popover-demo"><strong>{p.label}</strong><button ref={trigger} class="popover-trigger" aria-haspopup="dialog" aria-expanded={shown()} aria-controls={id} onClick={()=>setShown(v=>!v)}>Toggle</button>{shown()&&<div id={id} role="dialog" aria-label={p.label} class={'kumo-popover side-'+p.side+' align-'+p.align} style={{'--side-offset':(p.sideOffset??8)+'px','--align-offset':(p.alignOffset??0)+'px'}}><p>Popover content</p><button onClick={()=>setShown(false)}>Close</button></div>}</section>}`;
await writeFile('runtime/popover/react/src/main.jsx',react);await writeFile('runtime/popover/vue/src/Popover.vue',vue);await writeFile('runtime/popover/vue/src/main.js',`import{createApp,h}from'vue';import Popover from'./Popover.vue';${css}const cases=${cases};createApp({render:()=>h('main',{class:'popover-shell'},[h('h1','Popover'),h('div',{class:'popover-matrix'},cases.map(p=>h(Popover,p)))])}).mount('#app')`);await writeFile('runtime/popover/svelte/src/Popover.svelte',svelte);await writeFile('runtime/popover/svelte/src/App.svelte',`<script>import Popover from'./Popover.svelte';${css}const cases=${cases}</script><main class="popover-shell"><h1>Popover</h1><div class="popover-matrix">{#each cases as p}<Popover {...p}/>{/each}</div></main>`);await writeFile('runtime/popover/svelte/src/main.js',`import{mount}from'svelte';import App from'./App.svelte';mount(App,{target:document.getElementById('app')})`);await writeFile('runtime/popover/solid/src/Popover.jsx',solid);await writeFile('runtime/popover/solid/src/main.jsx',`import{render}from'solid-js/web';import Popover from'./Popover';${css}const cases=${cases};render(()=><main class="popover-shell"><h1>Popover</h1><div class="popover-matrix">{cases.map(p=><Popover {...p}/>)}</div></main>,document.getElementById('app'))`);
const ssr=spec.states.map(p=>`<section class="popover-demo"><strong>${p.label}</strong><button class="popover-trigger" aria-haspopup="dialog" aria-expanded="${!!p.open}" aria-controls="popover-${p.label.toLowerCase().replace(/\\W/g,'-')}">Toggle</button>${p.open?`<div id="popover-${p.label.toLowerCase().replace(/\\W/g,'-')}" role="dialog" aria-label="${p.label}" class="kumo-popover side-${p.side} align-${p.align}" style="--side-offset:${p.sideOffset??8}px;--align-offset:${p.alignOffset??0}px"><p>Popover content</p><button>Close</button></div>`:''}</section>`).join('');await writeFile('runtime/popover/react/index.html',`<div id="app"><main class="popover-shell"><h1>Popover</h1><div class="popover-matrix">${ssr}</div></main></div><script type="module" src="/src/main.jsx"></script>`);console.log('generated Popover from specs/popover.json');
