# Read-only MCP contract (proposal)
No server is implemented. Future tools: `kumo_manifest({root})`, `kumo_check({root})`, and `kumo_diff({root})`. Inputs must pass containment checks. Results are the same agent projection/drift JSON as the CLI. Tools expose no write primitive, return diagnostics separately, and MUST NOT invoke generators or lifecycle scripts.
