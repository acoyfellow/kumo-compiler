import{createComponent}from'solid-js';import Semantic from'./field.semantic';import Legacy from'./field.legacy';
const variants=[{"id":"optional","when":[{"kind":"fixture-equals","value":{"export":"root","props":{"label":"Name","description":"Help","required":false},"children":[{"export":".NativeInput","props":{"id":"field-control"},"children":[]}]}}]}];const equal=(a:unknown,b:unknown)=>JSON.stringify(a)===JSON.stringify(b);const content=(v:any):string=>Array.isArray(v)?v.map(content).join(''):v==null||typeof v==='boolean'?'':typeof v==='string'||typeof v==='number'?String(v):content(v.children);const matches=(when:any[],props:Record<string,any>)=>when.every((p:any)=>p.kind==='prop-equals'?equal(p.name==='children'?content(props.children):props[p.name],p.value):p.kind==='fixture-equals'?equal(props.fixture,p.value):false);export function Field(props:Record<string,any>){return createComponent(variants.some(v=>matches(v.when,props))?Semantic:Legacy,props)}export default Field;
