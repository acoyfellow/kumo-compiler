---
import CompilerStats from '../components/CompilerStats.astro';
import benchmark from '../../../benchmarks/compilers/zig.json';
const reproduce = `cd compilers/zig
zig build -Doptimize=ReleaseSmall
zig build test
./zig-out/bin/kumo-zig /absolute/path/request.json
cd ../..
npm run protocol:benchmark -- --candidate zig`;
---
<CompilerStats language="Zig" {benchmark} architecture="Standard-library-only native planner with no libc or package dependencies. It validates absolute roots and safe catalog references, generates all 164 artifacts in a sibling staging directory, then commits the complete tree by rename." binary="275,624 bytes (ReleaseSmall)" {reproduce} />
