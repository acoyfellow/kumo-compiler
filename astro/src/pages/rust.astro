---
import CompilerStats from '../components/CompilerStats.astro';
import benchmark from '../../../benchmarks/compilers/rust.json';
const reproduce = `cd compilers/rust
cargo build --release --offline
cargo test --offline
./target/release/kumo-rust-planner /absolute/path/request.json
cd ../..
npm run protocol:benchmark -- --candidate rust`;
---
<CompilerStats language="Rust" {benchmark} architecture="Protocol-v1 native planner that validates the request and kumo.ir/v1 catalog, creates an exact sorted target plan, and atomically promotes content-addressed artifacts. Tests cover malformed JSON, traversal, duplicate IDs, partial writes, and determinism." build="4.48 s wall on the benchmark host" binary="630,272 bytes (release)" {reproduce} />
