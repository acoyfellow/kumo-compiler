---
import CompilerStats from '../components/CompilerStats.astro';
import benchmark from '../../../benchmarks/compilers/go.json';
const reproduce = `cd compilers/go
go build -trimpath -o bin/kumo-go ./cmd/kumo-go
./bin/kumo-go /absolute/path/request.json
cd ../..
npm run protocol:benchmark -- --candidate go`;
---
<CompilerStats language="Go" {benchmark} architecture="Go standard-library protocol implementation; validates kumo.ir/v1, sorts the 41 × 4 plan, and atomically emits deterministic component/framework plan artifacts. The receipt is written to stdout." {reproduce} />
