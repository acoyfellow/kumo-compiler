# Evidence
28 bound receipts (7 components × 4 targets). `passed` means executable core/type/static boundary evidence only. All browser-dependent claims are `not-run`; nothing is waived. Run generator after tests to bind the current revision (or commit hash after committing and regenerate/amend).
