# Native Select evidence

Deterministic fail-closed evidence for candidate `shared-core-native-select`. Generate with `node candidates/shared-core/proof/select/run.mjs`. A gate is never promoted from source inspection: unavailable browser journeys remain `blocked`, and `test/select-native-evidence.test.mjs` rejects missing or optimistic gates. Adapter file and nonblank LOC counts are recorded per framework.
