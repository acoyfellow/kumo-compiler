import React,{useState} from 'react';
import {Select,SelectLabel,SelectValue,SelectDescription,SelectTrigger,SelectListbox,SelectOption} from '../../../candidates/shared-core/src/views/select/react/index.tsx';
const options=[['alpha','Alpha',false],['blocked','Blocked',true],['charlie','Charlie',false],['delta','Delta',false],['echo','Éclair',false]] as const;
export function App(){const [controlled,setControlled]=useState<string|null>(null),[open,setOpen]=useState(false);const log=(x:string)=>{(globalThis as any).__events.push(x)};return <main data-fixture-import="src/views/select/react/index.tsx"><section id="uncontrolled"><Select id="react-u" defaultValue="alpha" onValueChange={v=>log('u:value:'+v)} onOpenChange={v=>log('u:open:'+v)}><SelectLabel>Choice</SelectLabel><SelectTrigger><SelectValue placeholder="Choose"/></SelectTrigger><SelectDescription>Pick one</SelectDescription><SelectListbox>{options.map(([id,label,disabled],i)=><SelectOption key={id} id={id} value={id} label={label} disabled={disabled} order={i}>{label}</SelectOption>)}</SelectListbox></Select></section><section id="controlled"><Select id="react-c" value={controlled} open={open} onValueChange={v=>{log('c:value:'+v);setControlled(v)}} onOpenChange={v=>{log('c:open:'+v);setOpen(v)}}><SelectLabel>Controlled</SelectLabel><SelectTrigger><SelectValue placeholder="Choose"/></SelectTrigger><SelectDescription>Pick controlled</SelectDescription><SelectListbox>{options.map(([id,label,disabled],i)=><SelectOption key={id} id={'c-'+id} value={id} label={label} disabled={disabled} order={i}>{label}</SelectOption>)}</SelectListbox></Select></section></main>}
