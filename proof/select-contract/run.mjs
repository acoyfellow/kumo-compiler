import {execFileSync} from 'node:child_process';import {readFileSync,writeFileSync} from 'node:fs';const path=new URL('./receipt.json',import.meta.url);const receipt=JSON.parse(readFileSync(path));let status='passed',reason;try{execFileSync(process.execPath,['--test','test/select-contract.test.mjs'],{cwd:new URL('../..',import.meta.url),stdio:'inherit'})}catch(e){status='failed';reason=`exit ${e.status??1}`}receipt.run={command:'node --test test/select-contract.test.mjs',status,...(reason?{reason}:{})};writeFileSync(path,JSON.stringify(receipt,null,2)+'\n');if(status==='failed')process.exitCode=1;
