#!/usr/bin/env node
import { spawn } from 'node:child_process'
import { createHash, randomUUID } from 'node:crypto'
import { createServer } from 'node:http'
import { existsSync } from 'node:fs'
import { mkdir, mkdtemp, readFile, rm, writeFile } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { resolve, relative } from 'node:path'
import { build } from 'vite'
import vue from '@vitejs/plugin-vue'

const HERE=import.meta.dirname,ROOT=resolve(HERE,'../..'),OUT=resolve(HERE,'outputs'),H=resolve(HERE,'harness'),DIST=resolve(HERE,'.runtime')
const sha=x=>createHash('sha256').update(x).digest('hex'),sleep=n=>new Promise(r=>setTimeout(r,n))
const components={button:['default','disabled','loading'],checkbox:['unchecked','checked','indeterminate'],field:['default','error','disabled'],popover:['closed','open','dismissed']},viewports=[390,768,1440]
const chrome=process.env.CHROME_PATH||['/Applications/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing','/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'].find(existsSync)||'google-chrome'
async function connect(url){const ws=new WebSocket(url),pending=new Map(),events=[];let id=0;await new Promise((ok,no)=>{ws.onopen=ok;ws.onerror=no});ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id){const p=pending.get(m.id);pending.delete(m.id);m.error?p.no(Error(m.error.message)):p.ok(m.result)}else events.push(m)};return {events,send:(method,params={})=>new Promise((ok,no)=>{const n=++id;pending.set(n,{ok,no});ws.send(JSON.stringify({id:n,method,params}))}),close:()=>ws.close()}}
async function launch(){const profile=await mkdtemp(resolve(tmpdir(),'kumo-vue-'));const proc=spawn(chrome,['--headless=new','--disable-gpu','--hide-scrollbars','--no-first-run',`--user-data-dir=${profile}`,'--remote-debugging-port=0','about:blank'],{stdio:'ignore'});let port;for(let i=0;i<200;i++){try{port=Number((await readFile(resolve(profile,'DevToolsActivePort'),'utf8')).split('\n')[0]);break}catch{await sleep(25)}}if(!port)throw Error('Chrome unavailable');const version=await fetch(`http://127.0.0.1:${port}/json/version`).then(r=>r.json()),target=await fetch(`http://127.0.0.1:${port}/json/new?about%3Ablank`,{method:'PUT'}).then(r=>r.json());return {proc,profile,version,cdp:await connect(target.webSocketDebuggerUrl)}}
const evaluate=`(()=>{const round=n=>Math.round(n*1000)/1000,attrs=e=>Object.fromEntries([...e.attributes].map(a=>[a.name,a.value]).sort((a,b)=>a[0].localeCompare(b[0]))),esc=s=>s.replace(/[^a-zA-Z0-9_-]/g,c=>'_'+c.codePointAt(0).toString(16)),path=e=>{const a=[];for(let n=e;n&&n.nodeType===1;n=n.parentElement){let i=0;for(let p=n.previousElementSibling;p;p=p.previousElementSibling)i++;a.unshift(esc(n.localName)+':'+i);if(n===document.documentElement)break}return a.join('/')},main=document.querySelector('main'),owned=new Set(main?[main,...main.querySelectorAll('*')]:[]);for(const e of document.querySelectorAll('[data-part]'))owned.add(e);for(const e of [...owned])for(const key of (e.getAttribute('aria-controls')||'').split(/\\s+/).filter(Boolean)){const n=document.getElementById(key);if(n){owned.add(n);for(const d of n.querySelectorAll('*'))owned.add(d)}}for(const e of [...owned])if(main&&!main.contains(e))for(let n=e.parentElement;n&&n!==document.body;n=n.parentElement){owned.add(n);for(const d of n.querySelectorAll('*'))owned.add(d)};const all=[...owned].sort((a,b)=>a===b?0:a.compareDocumentPosition(b)&Node.DOCUMENT_POSITION_FOLLOWING?-1:1),id=e=>{const part=e.dataset.part?.replace(/^(?:part|slot):/,'');return part?'part:'+esc(part):'path:'+path(e)},nodes=all.map(e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e),p=e.parentElement;return {id:id(e),part:e.dataset.part||null,parentId:p&&owned.has(p)?id(p):null,order:p?[...p.children].indexOf(e):0,namespace:e.namespaceURI,tag:e.localName,role:e.getAttribute('role')||e.role||null,attrs:attrs(e),classes:[...e.classList].sort(),text:e.childElementCount?null:e.textContent.trim(),geometry:{x:round(r.x),y:round(r.y),width:round(r.width),height:round(r.height)},style:Object.fromEntries(['display','position','color','background-color','font-family','font-size','font-weight','border-radius','opacity','visibility'].map(k=>[k,s.getPropertyValue(k)]))}});return {url:location.href,hydrated:globalThis.__hydrated===true,dom:main.outerHTML,parts:nodes,focus:{active:document.activeElement&&owned.has(document.activeElement)?id(document.activeElement):document.activeElement?.localName},events:[...(globalThis.__events||[])]}})()`
async function snapshot(cdp){const evaluated=await cdp.send('Runtime.evaluate',{expression:evaluate,returnByValue:true});if(evaluated.exceptionDetails||!evaluated.result?.value)throw new Error(`snapshot failed: ${evaluated.exceptionDetails?.exception?.description??evaluated.exceptionDetails?.text??'no value'}`);const value=evaluated.result.value,a11y=(await cdp.send('Accessibility.getFullAXTree')).nodes.filter(n=>!n.ignored).map(n=>({role:n.role?.value,name:n.name?.value,checked:n.checked?.value,expanded:n.expanded?.value,disabled:n.disabled?.value})),png=Buffer.from((await cdp.send('Page.captureScreenshot',{format:'png',fromSurface:true})).data,'base64');return {...value,a11y,screenshot:{sha256:sha(png),bytes:png.length},png}}
async function run(){await rm(DIST,{recursive:true,force:true});await rm(OUT,{recursive:true,force:true});const config={root:H,plugins:[vue()],logLevel:'silent',build:{emptyOutDir:true}};await build({...config,build:{...config.build,outDir:resolve(DIST,'client'),rollupOptions:{input:resolve(H,'client.mjs')}}});await build({...config,build:{...config.build,outDir:resolve(DIST,'server'),ssr:resolve(H,'entry-server.mjs')}});const clientFiles=(await import('node:fs/promises')).readdir(resolve(DIST,'client'),{recursive:true});const clientName=(await clientFiles).find(x=>x.endsWith('.js')),client=await readFile(resolve(DIST,'client',clientName)),serverEntry=(await (await import('node:fs/promises')).readdir(resolve(DIST,'server'))).find(x=>x.endsWith('.mjs')||x.endsWith('.js')),renderer=await import(`${resolve(DIST,'server',serverEntry)}?t=${Date.now()}`),style=await readFile(resolve(ROOT,'../../node_modules/@cloudflare/kumo/dist/styles/kumo-standalone.css')),sourceFiles=['Button.vue','Checkbox.vue','Field.vue','Popover.vue'].map(x=>resolve(HERE,x)),generatedSourceDigest=sha(Buffer.concat(await Promise.all(sourceFiles.map(file=>readFile(file))))),lowererDigest=sha('hand-written-baseline'),compilerDigest=sha(await readFile(resolve(ROOT,'../../node_modules/@vue/compiler-sfc/dist/compiler-sfc.cjs.js'))),nativeBuildDigest=sha(Buffer.concat([client,await readFile(resolve(DIST,'server',serverEntry))])),harnessDigest=sha(Buffer.concat([await readFile(resolve(H,'App.vue')),await readFile(resolve(H,'client.mjs')),await readFile(resolve(H,'entry-server.mjs')),style,client]));const server=createServer(async(req,res)=>{const u=new URL(req.url,'http://localhost');if(u.pathname==='/app.js'){res.setHeader('content-type','text/javascript');return res.end(client)}if(u.pathname==='/style.css'){res.setHeader('content-type','text/css');return res.end(style)}const props={component:u.searchParams.get('component'),state:u.searchParams.get('state')},ssr=await renderer.render(props),encoded=JSON.stringify(props).replace(/'/g,'&#39;');res.setHeader('content-type','text/html');res.end(`<!doctype html><html><head><meta charset="utf-8"><link rel="stylesheet" href="/style.css"></head><body><div id="app" data-props='${encoded}'>${ssr}</div><script type="module" src="/app.js"></script></body></html>`)});await new Promise(r=>server.listen(0,'127.0.0.1',r));const origin=`http://127.0.0.1:${server.address().port}`,b=await launch(),records=[],diagnostics=[];try{const {cdp}=b;await Promise.all(['Page.enable','Runtime.enable','Accessibility.enable'].map(x=>cdp.send(x)));for(const [component,states] of Object.entries(components))for(const state of states)for(const viewport of viewports){await cdp.send('Emulation.setDeviceMetricsOverride',{width:viewport,height:720,deviceScaleFactor:1,mobile:false});const url=`${origin}/?component=${component}&state=${state}`;console.log('capture',component,state,viewport);await cdp.send('Page.navigate',{url});await sleep(150);const initial=await snapshot(cdp);const target=(await cdp.send('Runtime.evaluate',{expression:`(()=>{const e=document.querySelector('[data-part=control],[data-part=trigger]');if(!e)return null;const r=e.getBoundingClientRect();return {x:r.x+r.width/2,y:r.y+r.height/2,disabled:e.disabled,part:e.dataset.part?('part:'+e.dataset.part):null,type:'click'}})()`,returnByValue:true})).result.value;if(target&&!target.disabled){await cdp.send('Input.dispatchMouseEvent',{type:'mousePressed',x:target.x,y:target.y,button:'left',clickCount:1});await cdp.send('Input.dispatchMouseEvent',{type:'mouseReleased',x:target.x,y:target.y,button:'left',clickCount:1});await cdp.send('Input.dispatchKeyEvent',{type:'keyDown',key:'Tab',code:'Tab'});await cdp.send('Input.dispatchKeyEvent',{type:'keyUp',key:'Tab',code:'Tab'});await sleep(50)}const after=await snapshot(cdp),value=initial,png=initial.png,trace={schemaVersion:'kumo.visual-trace/v3',component,state,viewport,dom:initial.dom,parts:initial.parts,a11y:initial.a11y,screenshot:initial.screenshot,checkpoint:{initial:{...initial,png:undefined},after:{...after,png:undefined}},behavior:{action:target&&!target.disabled?{type:target.type,target:target.part}:null,before:{focus:initial.focus,events:initial.events},after:{focus:after.focus,events:after.events}}},traceBytes=Buffer.from(JSON.stringify(trace,null,2)+'\n'),dir=resolve(OUT,component,state,String(viewport));await mkdir(dir,{recursive:true});await writeFile(resolve(dir,'trace.json'),traceBytes);await writeFile(resolve(dir,'screenshot.png'),png);await writeFile(resolve(dir,'after.png'),after.png);const capturedAt=new Date().toISOString(),capture={driver:'CDP',trusted:true,independent:true,id:randomUUID(),capturedAt,canonicalArtifactUsed:false,buildDigest:nativeBuildDigest,harnessDigest},captureDigest=sha(JSON.stringify(capture));const provenance={schemaVersion:'kumo.native-harness-provenance/v1',target:'vue',generatedSourceDigest,lowererDigest,nativeCompilerDigest:compilerDigest,nativeBuildDigest,servedHarnessDigest:harnessDigest,captureDigest,traceDigest:sha(traceBytes),screenshotDigest:sha(png),afterScreenshotDigest:sha(after.png),canonicalSourceDigest:sha('not-read-by-native-vue-capture'),generatedBuild:{digest:nativeBuildDigest,framework:'vue',compiler:'@vue/compiler-sfc + Vite'},servedHarness:{url,buildDigest:nativeBuildDigest,ssr:true,hydrated:value.hydrated},capture};await writeFile(resolve(dir,'provenance.json'),JSON.stringify(provenance,null,2)+'\n');records.push({component,state,viewport,hydrated:value.hydrated,parts:value.parts.length})}diagnostics.push(...cdp.events.filter(x=>x.method==='Runtime.exceptionThrown'));cdp.close()}finally{b.proc.kill();server.close();try{await rm(b.profile,{recursive:true,force:true,maxRetries:5,retryDelay:100})}catch{}await rm(DIST,{recursive:true,force:true})}const result={schemaVersion:'kumo.vue-native-harness/v1',status:diagnostics.length||records.some(x=>!x.hydrated)?'failed':'passed',cells:records.length,expectedCells:36,ssrHydrated:records.every(x=>x.hydrated),trustedCDP:true,servedHttp:true,digests:{generatedSourceDigest,lowererDigest,compilerDigest,nativeBuildDigest,harnessDigest},records,diagnostics};await writeFile(resolve(HERE,'harness-results.json'),JSON.stringify(result,null,2)+'\n');console.log(`${result.status}: captured ${records.length}/36 Vue cells`);if(result.status!=='passed')process.exitCode=1}
run().catch(e=>{console.error(e.stack);process.exitCode=1})
