{
  "name": "kumo-fanout-site",
  "type": "module",
  "private": true,
  "scripts": {
    "data": "node build-data.mjs",
    "build": "node build-data.mjs && vite build",
    "dev": "node build-data.mjs && vite"
  }
}
