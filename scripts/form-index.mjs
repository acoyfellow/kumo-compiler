import{readFile,writeFile,cp,mkdir}from'node:fs/promises';for(const f of['react','vue','svelte','solid']){let s=await readFile(`runtime/form/${f}/index.html`,'utf8');s=s.replace('/src/main.js',`/form/${f}/assets/${f}-form.js`);await writeFile(`runtime/form/${f}/public-runtime/index.html`,s);await cp(`runtime/form/${f}/public-runtime`,`deploy/form/${f}`,{recursive:true})}await mkdir('deploy/form/compare',{recursive:true});await writeFile('deploy/form/compare/index.html','<!doctype html><title>Form family comparison</title><h1>Form family</h1><iframe src="/form/react" title="Form runtime"></iframe>')
