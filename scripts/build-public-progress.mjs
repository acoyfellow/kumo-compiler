import{readFile,writeFile,mkdir}from'node:fs/promises';import{createHash}from'node:crypto';import{resolve}from'node:path';const root=resolve(import.meta.dirname,'..'),json=async p=>JSON.parse(await readFile(resolve(root,p),'utf8')),status=await json('proof/observable-contracts/status.json'),canonical=await json('proof/observable-contracts/canonical.json'),library=await json('src/kumo/library/manifest.json'),packages=await json('library-artifacts/manifest.json');const packaged=[...new Set(packages.packages.flatMap(x=>x.components))],allFrameworks=packages.packages.length===3;const phases=[{id:'contracts',label:'Canonical contracts',done:status.counts.contracted,total:41,status:status.counts.contracted===41?'passed':'in-progress'},{id:'canonical',label:'Canonical browser vectors',done:canonical.counts.passed??0,total:canonical.cells.length,status:canonical.status},{id:'library-ir',label:'Implementation-ready library models',done:library.components.filter(x=>x.implementationReady===true).length,total:41,status:library.components.every(x=>x.implementationReady===true)?'passed':'in-progress'},{id:'packages',label:'Components in all three packages',done:allFrameworks?packaged.length:0,total:41,status:allFrameworks&&packaged.length===41?'passed':'in-progress'},{id:'packed-conformance',label:'Four-framework package conformance',done:0,total:41,status:'not-run'},{id:'examples-docs',label:'Complete component examples and docs',done:packaged.length,total:41,status:packaged.length===41?'passed':'in-progress'},{id:'production',label:'Final production proof',done:0,total:1,status:'not-run'}];const current=phases.find(x=>x.status!=='passed')?.id??'complete',report={schemaVersion:1,scope:{classified:45,executable:41,upstreamBlocked:['PageHeader','ResourceListPage'],supplemental:['Chart','Flow']},currentPhase:current,phases,packageNames:packages.packages.map(x=>x.package),humanOnly:['Git remote and GitHub release upload','Optional npm authentication and publish']};report.digest=createHash('sha256').update(JSON.stringify(report)).digest('hex');await mkdir(resolve(root,'proof/progress'),{recursive:true});await writeFile(resolve(root,'proof/progress/latest.json'),JSON.stringify(report,null,2)+'\n');const bar=x=>`${'█'.repeat(Math.round(20*x.done/x.total))}${'░'.repeat(20-Math.round(20*x.done/x.total))}`;const md=`# Kumo completion progress\n\nCurrent phase: **${current}**  \nMachine receipt: \`proof/progress/latest.json\`\n\n${phases.map(x=>`- ${x.status==='passed'?'✓':x.status==='in-progress'?'→':'○'} **${x.label}** — ${x.done}/${x.total}  \n  \`${bar(x)}\``).join('\n')}\n\n## Scope\n\n- 41 executable components must complete every gate.\n- PageHeader and ResourceListPage remain upstream-export blocked.\n- Chart and Flow are supplemental, not executable.\n\n## Current package surface\n\n${packaged.map(x=>`- ${x}`).join('\n')}\n\nThis file is generated from receipts. Do not edit it manually.\n`;await writeFile(resolve(root,'docs/progress.md'),md);console.log(`Progress: phase=${current}; contracts=${status.counts.contracted}/41; canonical=${canonical.counts.passed??0}/${canonical.cells.length}; packaged=${packaged.length}/41`);
