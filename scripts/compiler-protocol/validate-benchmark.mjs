#!/usr/bin/env node
import {readFile} from 'node:fs/promises';const x=JSON.parse(await readFile(process.argv[2]??new URL('../../benchmarks/compilers/typescript-baseline.json',import.meta.url),'utf8')),e=[];if(x.schemaVersion!=='kumo.compiler.benchmark/v1')e.push('version');if(x.methodology?.warmups<2||x.methodology?.measured<10)e.push('methodology');if(!Array.isArray(x.samples)||x.samples.length!==x.methodology.measured)e.push('samples');for(const s of x.samples??[])for(const k of ['wallMs','cpuUserMs','cpuSystemMs'])if(typeof s[k]!=='number'||s[k]<0)e.push(k);if(!Number.isFinite(x.summary?.medianWallMs)||!Number.isFinite(x.summary?.p95WallMs)||!Number.isFinite(x.summary?.varianceWallMs))e.push('summary');if(!Number.isInteger(x.output?.bytes)||!Number.isInteger(x.output?.files)||!/^[a-f0-9]{64}$/.test(x.output?.hash??''))e.push('output');if(e.length)throw new Error([...new Set(e)].join(', '));console.log(JSON.stringify({status:'ok',samples:x.samples.length,output:x.output}));
