<!-- @generated by src/kumo/emitters/vue/index.mjs; do not edit -->
<script lang="ts">
export const modelDigest = "2672fb3450a789e3a7035eb2076aac03fbe9e6e27a3f2df5f37d1b4a9e708c05"
export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd"
</script>

<script setup lang="ts">
defineOptions({ inheritAttrs: false })
import { computed, nextTick, onMounted, ref, useAttrs, useSlots } from 'vue'
interface CommandPaletteProps {
  "compound"?: unknown
  "Dialog"?: unknown
  "Input"?: unknown
  "Panel"?: string
  "Root"?: string
  "onHighlightChange"?: unknown
  "onValueChange"?: unknown
  "onOpenChange"?: unknown
  "highlights"?: unknown
  "text"?: unknown
  fixture?: unknown
  semanticContent?: unknown
}
const props = withDefaults(defineProps<CommandPaletteProps>(), {})
type CommandPaletteFixtureNode = { export?: string; text?: string; props?: Record<string, any>; children?: CommandPaletteFixtureNode[] }
const paletteFixture = computed(() => props.fixture as CommandPaletteFixtureNode | undefined)
const fixtureChildren = (node?: CommandPaletteFixtureNode) => node?.children ?? []
const fixturePart = (node: CommandPaletteFixtureNode | undefined, name: string) => fixtureChildren(node).find(child => child.export === name)
const partText = (node?: CommandPaletteFixtureNode): string => node ? String(node.text ?? '') + fixtureChildren(node).map(partText).join('') : ''
const highlightedText = computed(() => paletteFixture.value?.export === '.HighlightedText')
const highlightSegments = computed(() => {
  const text = String(paletteFixture.value?.props?.text ?? '')
  const ranges = (paletteFixture.value?.props?.highlights ?? []) as Array<[number, number]>
  const segments: Array<{ text: string; marked: boolean }> = []
  let cursor = 0
  for (const [start, end] of ranges.toSorted((a, b) => a[0] - b[0])) {
    if (start > cursor) segments.push({text: text.slice(cursor, start), marked: false})
    segments.push({text: text.slice(start, end + 1), marked: true})
    cursor = Math.max(cursor, end + 1)
  }
  if (cursor < text.length) segments.push({text: text.slice(cursor), marked: false})
  return segments
})
const inputPart = computed(() => fixturePart(paletteFixture.value, '.Input'))
const listPart = computed(() => fixturePart(paletteFixture.value, '.List'))
const items = computed(() => fixtureChildren(listPart.value).filter(item => item.export === '.Item'))
const inputRef = ref<HTMLInputElement | null>(null)
const open = ref(Boolean(paletteFixture.value?.props?.open))
const value = ref('')
const highlightedIndex = ref(items.value.length ? 0 : -1)
onMounted(() => { if (!highlightedText.value && highlightedIndex.value >= 0) props.onHighlightChange?.(String(items.value[highlightedIndex.value]?.props?.value ?? partText(items.value[highlightedIndex.value]))) })
function handleInput(event: Event) { value.value = (event.currentTarget as HTMLInputElement).value; props.onValueChange?.(value.value) }
function handlePaletteKey(event: KeyboardEvent) {
  if (event.key === 'ArrowDown' && items.value.length) {
    event.preventDefault()
    highlightedIndex.value = Math.min(highlightedIndex.value + 1, items.value.length - 1)
    props.onHighlightChange?.(String(items.value[highlightedIndex.value]?.props?.value ?? partText(items.value[highlightedIndex.value])))
  } else if (event.key === 'Escape') {
    event.preventDefault()
    open.value = false
    props.onOpenChange?.(false)
    inputRef.value?.blur()
    nextTick(() => (document.activeElement as HTMLElement | null)?.blur?.())
  }
}
const slots = useSlots()
const styles: Record<string,string> = {}
const normalizeSlotContent = (value: any): string => Array.isArray(value) ? value.map(normalizeSlotContent).join('') : value == null || typeof value === 'boolean' ? '' : typeof value === 'string' || typeof value === 'number' ? String(value) : normalizeSlotContent(value.children)
const renderContent = () => props.semanticContent ?? normalizeSlotContent(slots.default?.())
const fixture = computed(() => props.fixture)
const semanticValues = Object.assign({}, useAttrs(), props) as Record<string, unknown>
const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right)
const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : ''
</script>

<template>
  <span v-if="highlightedText"><template v-for="(segment, index) in highlightSegments" :key="index"><mark v-if="segment.marked">{{ segment.text }}</mark><template v-else>{{ segment.text }}</template></template></span><div v-else-if="open" data-base-ui-portal><div role="presentation" style="position:fixed;inset:0;user-select:none"></div><div role="presentation" class="fixed inset-0 bg-kumo-overlay opacity-80 transition-all duration-150 data-[ending-style]:opacity-0 data-[starting-style]:opacity-0"></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><div role="dialog" tabindex="-1" class="bg-kumo-base shadow-xs ring ring-kumo-line fixed top-[10vh] left-1/2 w-full max-w-2xl -translate-x-1/2 overflow-hidden rounded-lg duration-150 data-[ending-style]:scale-90 data-[ending-style]:opacity-0 data-[starting-style]:scale-90 data-[starting-style]:opacity-0" style="transition-property:scale,opacity;transition-timing-function:var(--default-transition-timing-function)"><div class="flex max-h-[60vh] flex-col overflow-hidden rounded-lg bg-kumo-elevated"><div class="flex items-center gap-3 bg-kumo-base px-4 py-3 ring-2 ring-kumo-brand focus-within:ring-2 focus-within:ring-kumo-brand" style="--tw-ring-color:var(--color-kumo-brand)"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" fill="currentColor" viewBox="0 0 256 256" class="h-4 w-4 text-kumo-subtle"><path d="M232.49,215.51,185,168a92.12,92.12,0,1,0-17,17l47.53,47.54a12,12,0,0,0,17-17ZM44,112a68,68,0,1,1,68,68A68.07,68.07,0,0,1,44,112Z"></path></svg><span role="button" aria-label="Dismiss" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:absolute"></span><input ref="inputRef" role="combobox" aria-expanded="true" aria-haspopup="listbox" aria-autocomplete="list" autocomplete="off" spellcheck="false" :placeholder="inputPart?.props?.placeholder" class="flex-1 border-none bg-transparent text-base kumo-input-placeholder outline-none" :value="value" @input="handleInput" @keydown="handlePaletteKey" /></div><div class="relative min-h-0 flex-1 overflow-y-auto rounded-b-lg bg-kumo-base px-2 py-2 scroll-py-2 ring-1 ring-kumo-hairline"><div v-for="(item, index) in items" :key="String(item.props?.value ?? index)" role="option" class="group flex w-full items-center gap-3 px-2 py-1.5 text-left text-base transition-colors cursor-pointer data-[highlighted]:bg-kumo-overlay rounded-lg" :data-highlighted="index === highlightedIndex ? '' : undefined">{{ partText(item) }}</div></div><input tabindex="-1" aria-hidden="true" :value="value" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0" /></div></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span></div>
</template>
