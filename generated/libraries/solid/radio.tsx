// @generated by src/kumo/emitters/solid/index.mjs; do not edit
import { For, createSignal, createUniqueId, mergeProps, splitProps } from "solid-js";
import type { JSX } from "solid-js";

export interface RadioProps extends Record<string, unknown> { children?: JSX.Element; fixture?: unknown; styles?: Record<string, string>; }
export interface CompoundPartProps extends JSX.HTMLAttributes<HTMLDivElement> { children?: JSX.Element; }
export const modelDigest = "0fb1bd718d40fbc0aecffd04f5e7e67ffb54bd77534c52ac02ff74e9f52dedc1";
export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
export const semanticVariantDigests = {} as const;
const styles: Record<string, string> = {"root":"root","rounded-full":"rounded-full","data-[checked]":"data-[checked]"};
const mergeStyles = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(" ").split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push("w"); if (/^h-/.test(t)) k.push("h"); if (/^size-/.test(t)) k.push("w","h"); if (/^p-/.test(t)) k.push("p","px","py","pt","pb","pl","pr"); if (/^px-/.test(t)) k.push("px","pl","pr"); if (/^py-/.test(t)) k.push("py","pt","pb"); if (/^pt-/.test(t)) k.push("pt"); if (/^pb-/.test(t)) k.push("pb"); if (/^pl-/.test(t)) k.push("pl"); if (/^pr-/.test(t)) k.push("pr"); if (/^rounded(-|$)/.test(t)) k.push("rounded"); if (/^gap-/.test(t)) k.push("gap"); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push("text-size"); if (/^justify-/.test(t)) k.push("justify"); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(" "); };
const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
const normalizeRenderContent = (value: unknown, accessors = false): string => {
  if (value == null || value === false || value === true) return "";
  if (typeof value === "string" || typeof value === "number") return String(value);
  if (Array.isArray(value)) return value.map(item => normalizeRenderContent(item, accessors)).join("");
  if (accessors && typeof value === "function") return normalizeRenderContent(value(), accessors);
  if (typeof value === "object") { const item = value as {text?: unknown; children?: unknown}; return (typeof item.text === "string" ? item.text : "") + (Array.isArray(item.children) ? item.children.map(child => normalizeRenderContent(child)).join("") : ""); }
  return "";
};
const normalizeFixture = (value: unknown): unknown => Array.isArray(value) ? value.map(normalizeFixture) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([key, item]) => [key, normalizeFixture(item)])) : value;
const fixtureText = (value: unknown): string => normalizeRenderContent(value);
const compoundFixtureText = (value: unknown, exported: string): string => {
  const visit = (item: unknown): string => {
    if (!item || typeof item !== "object") return "";
    const node = item as {export?: unknown; text?: unknown; children?: unknown[]};
    if (node.export === exported) return fixtureText(node);
    return Array.isArray(node.children) ? node.children.map(visit).join("") : "";
  };
  return visit(value);
};
const resolvePortalTarget = (target: unknown) => target === "document-body" && typeof document !== "undefined" ? document.body : target as Node;

export function Radio(incoming: RadioProps): JSX.Element {
  const props = mergeProps({"orientation":"vertical"}, incoming);
  const fixture = props.fixture;
  const renderContent = normalizeRenderContent(props.children, true);
  const normalizedFixture = normalizeFixture(fixture);
  const state: Record<string, () => unknown> = {};
  type RadioItem = {label: string; value: string; disabled?: boolean};
  type RadioFixture = {kind: "radio-group"; legend: string; items: RadioItem[]; defaultValue?: string; value?: string; disabled?: boolean};
  const radioFixture = () => (props.fixture as RadioFixture | undefined) ?? ({legend: "", items: []} as RadioFixture);
  const radioControlled = () => radioFixture().value !== undefined;
  const [radioValue, setRadioValue] = createSignal(radioFixture().defaultValue);
  const selectedValue = () => radioControlled() ? radioFixture().value : radioValue();
  const radioIdBase = createUniqueId();
  const radioLegendId = radioIdBase + "-legend";
  const radioItemId = (index: number) => radioIdBase + "-item-" + index;
  const radioInputId = (index: number) => radioIdBase + "-input-" + index;
  const radioLabelId = (index: number) => radioInputId(index) + "-label";
  let radioRoot: HTMLDivElement | undefined;
  const selectRadio = (item: RadioItem) => {
    if (radioFixture().disabled || item.disabled) return;
    if (!radioControlled()) setRadioValue(item.value);
    (props.onValueChange as ((value: string) => void) | undefined)?.(item.value);
    if (radioRoot) { radioRoot.setAttribute('tabindex', '-1'); radioRoot.focus(); }
  };
  const radioKeyDown = (event: KeyboardEvent & {currentTarget: HTMLSpanElement}, index: number) => {
    if (event.key !== "ArrowDown" || radioFixture().disabled) return;
    const items = radioFixture().items;
    const next = items.slice(index + 1).find(item => !item.disabled);
    if (next) { event.preventDefault(); selectRadio(next); }
  };
  const refs: Record<string, HTMLElement | undefined> = {};
  const [, native] = splitProps(props as RadioProps & Record<string, unknown>, []);
  void native; void state; void refs;
  return (<div ref={radioRoot} role="radiogroup"><fieldset class="flex flex-col gap-4" aria-labelledby={radioFixture().legend ? radioLegendId : undefined}>{radioFixture().legend ? <div id={radioLegendId} class="text-base font-medium text-kumo-default">{radioFixture().legend}</div> : undefined}<div class="flex flex-col gap-2"><For each={radioFixture().items} children={(item, index) => <label id={radioLabelId(index())} data-kumo-component="Radio" data-kumo-part="item-label" class="m-0 group relative inline-flex items-start gap-2 cursor-pointer"><span id={radioItemId(index())} data-kumo-component="Radio" data-kumo-part="item" class="relative mt-0.5 flex h-4 w-4 shrink-0 items-center justify-center rounded-full border-0 bg-kumo-base ring focus:outline-none after:absolute after:-inset-x-3 after:-inset-y-2 ring-kumo-line group-hover:ring-kumo-hairline focus:ring-kumo-focus focus:ring-2 focus-visible:ring-2 focus-visible:ring-kumo-brand focus-visible:outline-offset-3 data-[checked]:bg-kumo-contrast" role="radio" tabindex={selectedValue() === item.value && !(radioFixture().disabled || item.disabled) ? 0 : -1} aria-checked={selectedValue() === item.value} aria-labelledby={radioLabelId(index())} aria-disabled={Boolean(radioFixture().disabled || item.disabled) || undefined} data-composite-item-active={selectedValue() === item.value ? "" : undefined} data-checked={selectedValue() === item.value ? "" : undefined} data-unchecked={selectedValue() === item.value ? undefined : ""} onClick={event => { event.preventDefault(); selectRadio(item); }} onKeyDown={event => radioKeyDown(event, index())}><span class="flex items-center justify-center" data-checked={selectedValue() === item.value ? "" : undefined} data-unchecked={selectedValue() === item.value ? undefined : ""}><span class="h-2 w-2 rounded-full bg-kumo-base" /></span></span><input id={radioInputId(index())} type="radio" tabindex="-1" aria-hidden="true" value={item.value} attr:checked={selectedValue() === item.value ? "" : undefined} disabled={Boolean(radioFixture().disabled || item.disabled) || undefined} style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0" onChange={() => { if (selectedValue() !== item.value) selectRadio(item); }} /><span class="text-base text-kumo-default">{item.label}</span></label>} /></div></fieldset></div>);
}

export default Radio;
