// @generated by src/kumo/emitters/solid/index.mjs; do not edit
import { For, Show, createSignal, mergeProps, splitProps } from "solid-js";
import type { JSX } from "solid-js";

export interface ComboboxProps extends Record<string, unknown> { children?: JSX.Element; fixture?: unknown; styles?: Record<string, string>; }
export interface CompoundPartProps extends JSX.HTMLAttributes<HTMLDivElement> { children?: JSX.Element; }
export const modelDigest = "87e32915ebbd07b300ba0f9ea11293bed3a0e1d43b56400838cf3b0b3064be90";
export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
export const semanticVariantDigests = {} as const;
const styles: Record<string, string> = {"root":"root","bg-kumo-control":"bg-kumo-control","ring-kumo-line":"ring-kumo-line","data-highlighted:bg-kumo-overlay":"data-highlighted:bg-kumo-overlay","data-selected:font-medium":"data-selected:font-medium","w-full px-2 py-1 border-0 bg-inherit":"w-full px-2 py-1 border-0 bg-inherit","flex items-center flex-wrap gap-1.5 flex-1":"flex items-center flex-wrap gap-1.5 flex-1","min-w-[100px]":"min-w-[100px]"};
const mergeStyles = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(" ").split(/\s+/).filter(Boolean); const groupOf = (t: string) => /^w-/.test(t) ? "w" : /^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t) ? "text-size" : null; const last: Record<string, number> = {}; tokens.forEach((t, i) => { const g = groupOf(t); if (g) last[g] = i; }); return tokens.filter((t, i) => { const g = groupOf(t); return !g || last[g] === i; }).join(" "); };
const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
const normalizeRenderContent = (value: unknown, accessors = false): string => {
  if (value == null || value === false || value === true) return "";
  if (typeof value === "string" || typeof value === "number") return String(value);
  if (Array.isArray(value)) return value.map(item => normalizeRenderContent(item, accessors)).join("");
  if (accessors && typeof value === "function") return normalizeRenderContent(value(), accessors);
  if (typeof value === "object") { const item = value as {text?: unknown; children?: unknown}; return (typeof item.text === "string" ? item.text : "") + (Array.isArray(item.children) ? item.children.map(child => normalizeRenderContent(child)).join("") : ""); }
  return "";
};
const normalizeFixture = (value: unknown): unknown => Array.isArray(value) ? value.map(normalizeFixture) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([key, item]) => [key, normalizeFixture(item)])) : value;
const fixtureText = (value: unknown): string => normalizeRenderContent(value);
const compoundFixtureText = (value: unknown, exported: string): string => {
  const visit = (item: unknown): string => {
    if (!item || typeof item !== "object") return "";
    const node = item as {export?: unknown; text?: unknown; children?: unknown[]};
    if (node.export === exported) return fixtureText(node);
    return Array.isArray(node.children) ? node.children.map(visit).join("") : "";
  };
  return visit(value);
};
const resolvePortalTarget = (target: unknown) => target === "document-body" && typeof document !== "undefined" ? document.body : target as Node;

export function Combobox(incoming: ComboboxProps): JSX.Element {
  const props = mergeProps({}, incoming);
  const fixture = props.fixture;
  const renderContent = normalizeRenderContent(props.children, true);
  const normalizedFixture = normalizeFixture(fixture);
  const state: Record<string, () => unknown> = {};
  type ComboboxFixtureNode = {export?: string; props: Record<string, unknown>; text?: string; children?: ComboboxFixtureNode[]};
  const comboboxFixture = () => props.fixture as ComboboxFixtureNode;
  const findComboboxPart = (exported: string, value: ComboboxFixtureNode | undefined = comboboxFixture()): ComboboxFixtureNode | undefined => value?.export === exported ? value : value?.children?.map(child => findComboboxPart(exported, child)).find(Boolean);
  const comboboxTrigger = () => findComboboxPart(".TriggerInput") ?? {props:{}};
  const comboboxItems = () => {
    const list = findComboboxPart(".List");
    return (list?.children ?? []).filter(item => item.export === ".Item").map(item => ({...item, value:String(item.props.value ?? "")}));
  };
  const [uncontrolledComboboxOpen, setUncontrolledComboboxOpen] = createSignal(Boolean(incoming.defaultOpen));
  const comboboxOpen = () => incoming.open !== undefined ? Boolean(props.open) : uncontrolledComboboxOpen();
  const [uncontrolledComboboxValue, setUncontrolledComboboxValue] = createSignal(String(incoming.defaultValue ?? ""));
  const comboboxValue = () => incoming.value !== undefined ? String(props.value) : uncontrolledComboboxValue();
  const [comboboxQuery, setComboboxQuery] = createSignal(comboboxValue());
  const filteredComboboxItems = () => { const query = comboboxQuery().trim().toLocaleLowerCase(); return query ? comboboxItems().filter(item => fixtureText(item).toLocaleLowerCase().includes(query)) : comboboxItems(); };
  const [highlightedIndex, setHighlightedIndex] = createSignal(-1);
  let comboboxInput: HTMLInputElement | undefined;
  const setComboboxOpen = (next: boolean) => { if (incoming.open === undefined) setUncontrolledComboboxOpen(next); (props.onOpenChange as ((open: boolean) => void) | undefined)?.(next); };
  const openCombobox = () => { setComboboxOpen(true); comboboxInput?.focus(); };
  const selectComboboxItem = (item: ComboboxFixtureNode & {value: string}, event?: PointerEvent) => { if (item.props.disabled) { event?.preventDefault(); return; } event?.preventDefault(); if (incoming.value === undefined) setUncontrolledComboboxValue(item.value); setComboboxQuery(item.value); (props.onValueChange as ((value: string) => void) | undefined)?.(item.value); setComboboxOpen(false); queueMicrotask(() => comboboxInput?.focus()); };
  const comboboxOnInput: JSX.EventHandlerUnion<HTMLInputElement, InputEvent> = event => { setComboboxQuery(event.currentTarget.value); setHighlightedIndex(-1); if (!comboboxOpen()) setComboboxOpen(true); };
  const comboboxKeyDown: JSX.EventHandlerUnion<HTMLInputElement, KeyboardEvent> = event => {
    if (event.key === "ArrowDown") { event.preventDefault(); if (!comboboxOpen()) setComboboxOpen(true); setHighlightedIndex(index => Math.min(index + 1, filteredComboboxItems().length - 1)); }
    else if (event.key === "ArrowUp") { event.preventDefault(); setHighlightedIndex(index => Math.max(index - 1, 0)); }
    else if (event.key === "Enter" && highlightedIndex() >= 0) { event.preventDefault(); const item = filteredComboboxItems()[highlightedIndex()]; if (item) selectComboboxItem(item); }
    else if (event.key === "Escape" && comboboxOpen()) { event.preventDefault(); setComboboxOpen(false); comboboxInput?.focus(); }
  };
  const refs: Record<string, HTMLElement | undefined> = {};
  const [, native] = splitProps(props as ComboboxProps & Record<string, unknown>, []);
  void native; void state; void refs;
  return (incoming.open === undefined && incoming.defaultOpen === undefined && !comboboxOpen() ? <><div class="relative inline-block w-full max-w-xs has-[:disabled]:opacity-50 has-[:disabled]:cursor-not-allowed"><input ref={comboboxInput} autocomplete="off" spellcheck="false" autocorrect="off" autocapitalize="none" role="combobox" aria-expanded="false" aria-haspopup="listbox" aria-autocomplete="list" type="text" placeholder={comboboxTrigger().props.placeholder as string} class="border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled h-9 gap-1.5 rounded-lg px-3 text-base focus:ring-kumo-focus/50 focus:ring-[1.5px] w-full pr-12 disabled:cursor-not-allowed" attr:value={comboboxQuery()} onClick={openCombobox} onFocus={openCombobox} onInput={comboboxOnInput} onKeyDown={comboboxKeyDown} /><button type="button" data-placeholder="" tabindex="0" aria-expanded="false" aria-haspopup="dialog" data-kumo-component="Combobox" data-kumo-part="trigger" aria-label="Show options" class="absolute top-1/2 -translate-y-1/2 flex items-center justify-center cursor-pointer text-kumo-subtle m-0 bg-transparent p-0 right-2" onClick={openCombobox} onKeyDown={comboboxKeyDown}><span aria-hidden="true" class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256" class="fill-current"><path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z"></path></svg></span></button></div><input style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0" tabindex="-1" aria-hidden="true" attr:value={comboboxQuery()} /></> : <><input ref={comboboxInput} role="combobox" aria-expanded={comboboxOpen()} aria-haspopup="listbox" aria-autocomplete="list" placeholder={comboboxTrigger().props.placeholder as string} style="overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:0;position:fixed;top:0;left:0" value={comboboxQuery()} onClick={openCombobox} onInput={comboboxOnInput} onKeyDown={comboboxKeyDown} /><Show when={comboboxOpen() && filteredComboboxItems().length > 0} children={<ul role="listbox"><For each={filteredComboboxItems()} children={(item, index) => <li role="option" tabindex="-1" data-value={item.value} aria-selected={highlightedIndex() === index()} aria-disabled={Boolean(item.props.disabled) || undefined} onPointerDown={event => selectComboboxItem(item, event)}>{fixtureText(item)}</li>} /></ul>} /></>);
}

export function ComboboxContent(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Content">{local.children}</div>;
}

export function ComboboxItem(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Item">{local.children}</div>;
}

export function ComboboxList(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="List">{local.children}</div>;
}

export function ComboboxTriggerInput(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="TriggerInput">{local.children}</div>;
}

Object.defineProperty(Combobox, "Content", {value:ComboboxContent, enumerable:true});
Object.defineProperty(Combobox, "Item", {value:ComboboxItem, enumerable:true});
Object.defineProperty(Combobox, "List", {value:ComboboxList, enumerable:true});
Object.defineProperty(Combobox, "TriggerInput", {value:ComboboxTriggerInput, enumerable:true});

export default Combobox;
