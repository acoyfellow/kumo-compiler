// @generated by src/kumo/emitters/solid/index.mjs; do not edit
import { For, Show, createSignal, mergeProps, onCleanup, onMount, splitProps } from "solid-js";
import { Portal } from "solid-js/web";
import type { JSX } from "solid-js";

export interface DropdownMenuProps extends Record<string, unknown> { children?: JSX.Element; fixture?: unknown; styles?: Record<string, string>; }
export interface CompoundPartProps extends JSX.HTMLAttributes<HTMLDivElement> { children?: JSX.Element; }
export const modelDigest = "3e6c8ea8ec001de4a46d730f7438828c90bcd87db3a503cf0dd39f64a905af84";
export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
export const semanticVariantDigests = {"closed-trigger-ssr":"96a51ef90c52bda16064391fbaddcdd8221a7f254583a75aa51732efd36fee89"} as const;
const styles: Record<string, string> = {"root":"root","data-highlighted":"data-highlighted","data-disabled":"data-disabled","data-popup-open":"data-popup-open","data-starting-style":"data-starting-style","data-ending-style":"data-ending-style","text-kumo-danger":"text-kumo-danger"};
const mergeStyles = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(" ").split(/\s+/).filter(Boolean); const groupOf = (t: string) => /^w-/.test(t) ? "w" : /^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t) ? "text-size" : null; const last: Record<string, number> = {}; tokens.forEach((t, i) => { const g = groupOf(t); if (g) last[g] = i; }); return tokens.filter((t, i) => { const g = groupOf(t); return !g || last[g] === i; }).join(" "); };
const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
const normalizeRenderContent = (value: unknown, accessors = false): string => {
  if (value == null || value === false || value === true) return "";
  if (typeof value === "string" || typeof value === "number") return String(value);
  if (Array.isArray(value)) return value.map(item => normalizeRenderContent(item, accessors)).join("");
  if (accessors && typeof value === "function") return normalizeRenderContent(value(), accessors);
  if (typeof value === "object") { const item = value as {text?: unknown; children?: unknown}; return (typeof item.text === "string" ? item.text : "") + (Array.isArray(item.children) ? item.children.map(child => normalizeRenderContent(child)).join("") : ""); }
  return "";
};
const normalizeFixture = (value: unknown): unknown => Array.isArray(value) ? value.map(normalizeFixture) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([key, item]) => [key, normalizeFixture(item)])) : value;
const fixtureText = (value: unknown): string => normalizeRenderContent(value);
const compoundFixtureText = (value: unknown, exported: string): string => {
  const visit = (item: unknown): string => {
    if (!item || typeof item !== "object") return "";
    const node = item as {export?: unknown; text?: unknown; children?: unknown[]};
    if (node.export === exported) return fixtureText(node);
    return Array.isArray(node.children) ? node.children.map(visit).join("") : "";
  };
  return visit(value);
};
const resolvePortalTarget = (target: unknown) => target === "document-body" && typeof document !== "undefined" ? document.body : target as Node;

export function DropdownMenu(incoming: DropdownMenuProps): JSX.Element {
  const props = mergeProps({}, incoming);
  const fixture = props.fixture;
  const renderContent = normalizeRenderContent(props.children, true);
  const normalizedFixture = normalizeFixture(fixture);
  const state: Record<string, () => unknown> = {};
  type DropdownFixtureNode = {export?: string; props?: Record<string, unknown>; text?: string; children?: DropdownFixtureNode[]};
  const dropdownFixture = () => props.fixture as DropdownFixtureNode;
  const dropdownCanonicalOverlay = () => dropdownFixture()?.export === undefined;
  const findDropdownPart = (exported: string, value: DropdownFixtureNode = dropdownFixture()): DropdownFixtureNode | undefined => value?.export === exported ? value : value?.children?.map(child => findDropdownPart(exported, child)).find(Boolean);
  const dropdownTriggerText = () => fixtureText(findDropdownPart(".Trigger")) || "Actions";
  const dropdownTriggerHasIcon = () => { const icon = findDropdownPart(".Trigger")?.props?.icon; return icon === "PlusIcon" || (typeof icon === "object" && (icon as DropdownFixtureNode)?.export === "PlusIcon"); };
  const dropdownItems = () => (findDropdownPart(".Content")?.children ?? []).map(node => node.export === ".Sub" ? findDropdownPart(".SubTrigger", node)! : node).filter(node => node?.export === ".Item" || node?.export === ".SubTrigger").map(node => ({node, text:fixtureText(node), disabled:Boolean(node.props?.disabled), submenu:node.export === ".SubTrigger"}));
  const dropdownSubItems = () => (findDropdownPart(".SubContent")?.children ?? []).filter(node => node.export === ".Item").map(node => ({text:fixtureText(node)}));
  const controlled = incoming.open !== undefined;
  const [uncontrolledOpen, setUncontrolledOpen] = createSignal(Boolean(incoming.defaultOpen));
  const [submenuOpen, setSubmenuOpen] = createSignal(false);
  const dropdownOpen = () => controlled ? Boolean(props.open) : uncontrolledOpen();
  let dropdownTrigger: HTMLButtonElement | undefined;
  let dropdownMenu: HTMLDivElement | undefined;
  const relocateDropdownPortal = (node: HTMLDivElement) => {
    let disposed = false;
    // Solid invokes refs while constructing a Show branch, before the branch is
    // inserted at its marker. Relocating synchronously lets that insertion move
    // the node back inline, so defer until the branch's DOM insertion completes.
    queueMicrotask(() => { if (!disposed && typeof document !== "undefined") document.body.appendChild(node); });
    onCleanup(() => { disposed = true; node.remove(); });
  };
  const setDropdownOpen = (next: boolean) => { if (!controlled) setUncontrolledOpen(next); (props.onOpenChange as ((open: boolean) => void) | undefined)?.(next); };
  const focusFirstDropdownItem = () => queueMicrotask(() => dropdownMenu?.querySelector<HTMLElement>('[role="menuitem"]:not([data-disabled])')?.focus());
  const closeDropdown = () => { setSubmenuOpen(false); setDropdownOpen(false); queueMicrotask(() => dropdownTrigger?.focus()); };
  const toggleDropdownFromPointer = () => { const next = !dropdownOpen(); setDropdownOpen(next); if (next) focusFirstDropdownItem(); };
  const dropdownTriggerKeyDown = (event: KeyboardEvent) => { if (event.key === "Escape" && dropdownOpen()) { event.preventDefault(); closeDropdown(); return; } if (event.key !== "ArrowDown") return; event.preventDefault(); setDropdownOpen(true); focusFirstDropdownItem(); };
  const dropdownDocumentKeyDown = (event: KeyboardEvent) => { if (event.defaultPrevented || event.key !== "Escape" || !dropdownOpen()) return; event.preventDefault(); closeDropdown(); };
  onMount(() => { document.addEventListener("keydown", dropdownDocumentKeyDown); onCleanup(() => document.removeEventListener("keydown", dropdownDocumentKeyDown)); });
  const selectDropdownItem = (item: {text: string; disabled: boolean}, event: MouseEvent) => { if (item.disabled) { event.preventDefault(); return; } (props.onSelect as ((value: string) => void) | undefined)?.(item.text); setDropdownOpen(false); setDropdownOpen(false); queueMicrotask(() => (document.activeElement as HTMLElement | null)?.blur()); };
  const dropdownItemKeyDown = (event: KeyboardEvent, item: {text: string; disabled: boolean; submenu: boolean}) => { if (event.key === "Escape") { event.preventDefault(); closeDropdown(); return; } if (event.key === "ArrowRight") { event.preventDefault(); setSubmenuOpen(true); queueMicrotask(() => document.querySelector<HTMLElement>('[role="menu"]:last-of-type [role="menuitem"]')?.focus()); return; } if (event.key.length === 1) { const target = dropdownItems().find(candidate => !candidate.disabled && candidate.text.toLowerCase().startsWith(event.key.toLowerCase())); if (target) queueMicrotask(() => [...document.querySelectorAll<HTMLElement>('[role="menuitem"]')].find(element => element.textContent === target.text)?.focus()); } };
  const refs: Record<string, HTMLElement | undefined> = {};
  const [, native] = splitProps(props as DropdownMenuProps & Record<string, unknown>, []);
  void native; void state; void refs;
  return (<><Show when={dropdownOpen()} children={<span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span>} /><button ref={dropdownTrigger} id="kumo-dropdown-trigger" type="button" tabindex="0" aria-haspopup="menu" aria-expanded={false} data-kumo-component={dropdownOpen() || dropdownTriggerHasIcon() ? "Button" : undefined} class={dropdownOpen() || dropdownTriggerHasIcon() ? "group flex w-max shrink-0 items-center font-medium select-none border-0 shadow-xs focus:outline-none focus:ring-kumo-focus/50 focus-visible:ring-2 focus-visible:ring-kumo-brand cursor-pointer disabled:cursor-not-allowed disabled:text-kumo-subtle h-9 gap-1.5 rounded-lg px-3 text-base bg-kumo-base !text-kumo-default ring not-disabled:hover:bg-kumo-tint disabled:bg-kumo-base/50 disabled:!text-kumo-default/70 ring-kumo-line data-[state=open]:bg-kumo-base" : undefined} onClick={toggleDropdownFromPointer} onKeyDown={dropdownTriggerKeyDown}>{dropdownTriggerHasIcon() ? <><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" fill="currentColor" viewBox="0 0 256 256"><path d="M224,128a8,8,0,0,1-8,8H136v80a8,8,0,0,1-16,0V136H40a8,8,0,0,1,0-16h80V40a8,8,0,0,1,16,0v80h80A8,8,0,0,1,224,128Z"></path></svg><span class="contents">{dropdownTriggerText()}</span></> : dropdownTriggerText()}</button><Show when={dropdownOpen() && dropdownCanonicalOverlay()} children={<><span aria-hidden="true" tabindex="0" data-base-ui-focus-guard="" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><span data-type="outside" aria-hidden="true" tabindex="0" data-base-ui-focus-guard="" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><span aria-owns="kumo-dropdown-menu" style="clip-path:inset(50%);position:fixed;top:0;left:0"></span><span data-type="outside" aria-hidden="true" tabindex="0" data-base-ui-focus-guard="" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span></>} /><Show when={dropdownOpen()} children={<><div ref={relocateDropdownPortal} id="kumo-dropdown-menu" data-base-ui-portal=""><div role="presentation" style="position:absolute;left:0;top:0;transform:translate(-19px,44px)"><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><div ref={dropdownMenu} role="menu" aria-labelledby="kumo-dropdown-trigger" class="overflow-hidden bg-kumo-control text-kumo-default rounded-lg shadow-lg ring ring-kumo-line min-w-36 p-1.5 data-[state=open]:animate-in data-[state=open]:fade-in-0 data-[state=open]:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 data-[state=closed]:animate-out data-[state=closed]:fade-out-0 data-[state=closed]:zoom-out-95"><For each={dropdownItems()} children={item => <div role="menuitem" tabindex="-1" aria-disabled={item.disabled || undefined} data-kumo-component="DropdownMenu" data-kumo-part="item" data-disabled={item.disabled ? "" : undefined} class="relative flex cursor-default items-center rounded-md px-2 py-1.5 text-base outline-hidden select-none focus:text-kumo-default focus:ring-kumo-focus/50 focus-visible:ring-2 focus-visible:ring-kumo-brand data-disabled:pointer-events-none data-disabled:opacity-50 data-highlighted:bg-kumo-overlay" onClick={event => selectDropdownItem(item, event)} onKeyDown={event => dropdownItemKeyDown(event, item)}>{item.text}</div>} /></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span></div></div></>} /></>);
}

export function DropdownMenuContent(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Content">{local.children}</div>;
}

export function DropdownMenuItem(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Item">{local.children}</div>;
}

export function DropdownMenuSub(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Sub">{local.children}</div>;
}

export function DropdownMenuSubContent(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="SubContent">{local.children}</div>;
}

export function DropdownMenuSubTrigger(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="SubTrigger">{local.children}</div>;
}

export function DropdownMenuTrigger(props: CompoundPartProps): JSX.Element {
  const [local, native] = splitProps(props, ["children"]);
  return <div {...native} data-kumo-part="Trigger">{local.children}</div>;
}

Object.defineProperty(DropdownMenu, "Content", {value:DropdownMenuContent, enumerable:true});
Object.defineProperty(DropdownMenu, "Item", {value:DropdownMenuItem, enumerable:true});
Object.defineProperty(DropdownMenu, "Sub", {value:DropdownMenuSub, enumerable:true});
Object.defineProperty(DropdownMenu, "SubContent", {value:DropdownMenuSubContent, enumerable:true});
Object.defineProperty(DropdownMenu, "SubTrigger", {value:DropdownMenuSubTrigger, enumerable:true});
Object.defineProperty(DropdownMenu, "Trigger", {value:DropdownMenuTrigger, enumerable:true});

export default DropdownMenu;
