// @generated by src/kumo/emitters/solid/index.mjs; do not edit
import { splitProps } from "solid-js";
import type { JSX } from "solid-js";

export interface InputAreaProps extends Record<string, unknown> { children?: JSX.Element; fixture?: unknown; styles?: Record<string, string>; }
export interface CompoundPartProps extends JSX.HTMLAttributes<HTMLDivElement> { children?: JSX.Element; }
export const modelDigest = "e1dc1bf751aaa7b6e3de69ce1b8a6870a6512d40db1fc175431f5c242ebf827e";
export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
export const semanticVariantDigests = {"bare":"ebf5bf8b27301ff66eaf323b56e6a9ac48972ee15edb2d88ae114e04d29c04cd"} as const;
const styles: Record<string, string> = {"root":"root"};
const mergeStyles = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(" ").split(/\s+/).filter(Boolean); const groupOf = (t: string) => /^w-/.test(t) ? "w" : /^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t) ? "text-size" : null; const last: Record<string, number> = {}; tokens.forEach((t, i) => { const g = groupOf(t); if (g) last[g] = i; }); return tokens.filter((t, i) => { const g = groupOf(t); return !g || last[g] === i; }).join(" "); };
const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
const normalizeRenderContent = (value: unknown, accessors = false): string => {
  if (value == null || value === false || value === true) return "";
  if (typeof value === "string" || typeof value === "number") return String(value);
  if (Array.isArray(value)) return value.map(item => normalizeRenderContent(item, accessors)).join("");
  if (accessors && typeof value === "function") return normalizeRenderContent(value(), accessors);
  if (typeof value === "object") { const item = value as {text?: unknown; children?: unknown}; return (typeof item.text === "string" ? item.text : "") + (Array.isArray(item.children) ? item.children.map(child => normalizeRenderContent(child)).join("") : ""); }
  return "";
};
const normalizeFixture = (value: unknown): unknown => Array.isArray(value) ? value.map(normalizeFixture) : value && typeof value === "object" ? Object.fromEntries(Object.entries(value).map(([key, item]) => [key, normalizeFixture(item)])) : value;
const fixtureText = (value: unknown): string => normalizeRenderContent(value);
const compoundFixtureText = (value: unknown, exported: string): string => {
  const visit = (item: unknown): string => {
    if (!item || typeof item !== "object") return "";
    const node = item as {export?: unknown; text?: unknown; children?: unknown[]};
    if (node.export === exported) return fixtureText(node);
    return Array.isArray(node.children) ? node.children.map(visit).join("") : "";
  };
  return visit(value);
};
const resolvePortalTarget = (target: unknown) => target === "document-body" && typeof document !== "undefined" ? document.body : target as Node;

export function InputArea(incoming: InputAreaProps): JSX.Element {
  const props = Object.assign({}, incoming);
  const fixture = props.fixture;
  const renderContent = normalizeRenderContent(props.children, true);
  const normalizedFixture = normalizeFixture(fixture);
  const state: Record<string, () => unknown> = {};
  const nativeInputHandler: JSX.EventHandlerUnion<HTMLTextAreaElement, InputEvent> = event => {
    (props.onInput as ((value: string) => void) | undefined)?.(event.currentTarget.value);
  };
  const refs: Record<string, HTMLElement | undefined> = {};
  const [, native] = splitProps(props as InputAreaProps & Record<string, unknown>, ["class","defaultValue","value","error","variant","disabled","onInput","children","fixture","styles"]);
  void native; void state; void refs;
  return (props.label != null ? <div class="grid gap-2 has-[input[type=checkbox]]:grid-cols-[auto_1fr] has-[input[type=checkbox]]:items-center has-[[role=switch]]:grid-cols-[auto_1fr] has-[[role=switch]]:items-center"><label id="kumo-b7ff621d139b-label" for="kumo-b7ff621d139b" class="m-0 select-none text-base font-medium text-kumo-default"><span class="inline-flex items-center gap-1">{props.label as JSX.Element}</span></label><textarea {...native} id="kumo-b7ff621d139b" aria-labelledby="kumo-b7ff621d139b-label" class={mergeStyles((props.error || props.variant === "error") ? "border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled gap-1.5 rounded-lg px-3 text-base !ring-kumo-danger focus:ring-kumo-danger/50 focus:ring-[1.5px] h-auto py-2" : "border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled gap-1.5 rounded-lg px-3 text-base focus:ring-kumo-focus/50 focus:ring-[1.5px] h-auto py-2", styles.root, props.class)} disabled={Boolean(props.disabled)} onInput={nativeInputHandler}>{(props.value !== undefined ? props.value : props.defaultValue) as string | number | string[] | undefined}</textarea></div> : <textarea {...native} class={mergeStyles((props.error || props.variant === "error") ? "border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled gap-1.5 rounded-lg px-3 text-base !ring-kumo-danger focus:ring-kumo-danger/50 focus:ring-[1.5px] h-auto py-2" : "border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled gap-1.5 rounded-lg px-3 text-base focus:ring-kumo-focus/50 focus:ring-[1.5px] h-auto py-2", styles.root, props.class)} disabled={Boolean(props.disabled)} onInput={nativeInputHandler}>{(props.value !== undefined ? props.value : props.defaultValue) as string | number | string[] | undefined}</textarea>);
}

export default InputArea;
