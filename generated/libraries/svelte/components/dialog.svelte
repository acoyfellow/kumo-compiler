<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "9d859aa9ef31faf6c5c78803e543a74a234ad027bc2f6557fc7aab5477498168";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  Close?: Snippet;
  Description?: Snippet;
  Dialog?: Snippet;
  Root?: Snippet;
  Title?: Snippet;
  Trigger?: Snippet;
  dialog?: Snippet;
  children?: Snippet;
  open?: boolean;
  defaultOpen?: boolean;
  onOpenChange?: (value: boolean) => void;
  triggerText?: unknown;
  title?: unknown;
  description?: unknown;
  closeText?: unknown;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    Close = undefined,
    Description = undefined,
    Dialog = undefined,
    Root = undefined,
    Title = undefined,
    Trigger = undefined,
    dialog = undefined,
    open = undefined,
    defaultOpen = false,
    onOpenChange = undefined,
    triggerText = "Open",
    title = "Dialog",
    description = undefined,
    closeText = "Close",
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_open = $state("Base UI controlled/defaultOpen; default closed");
  let state_role = $state("dialog");
  let state_size = $state("base");

  type DialogFixtureNode = { export?: string; text?: string; children?: DialogFixtureNode[] };
  type DialogFixture = { children?: DialogFixtureNode[] };
  const dialogFixture = $derived.by(() => { const root = fixture as DialogFixture | undefined; const nodes = root?.children ?? []; const find = (part: string) => nodes.flatMap(node => node.children ?? []).find(node => node.export === part); const text = (node: DialogFixtureNode | undefined): string | undefined => node?.children?.map(child => child.text ?? text(child) ?? '').join(''); return { triggerText: text(nodes.find(node => node.export === '.Trigger')), title: text(find('.Title')), description: text(find('.Description')), closeText: text(find('.Close')) }; });
  const controlledDialog = $derived(open !== undefined);
  let uncontrolledDialogOpen = $state(Boolean(defaultOpen));
  const currentDialogOpen = $derived(controlledDialog ? Boolean(open) : uncontrolledDialogOpen);
  let dialogTrigger: HTMLButtonElement | undefined = $state();
  let dialogContent: HTMLElement | undefined = $state();
  function portal(node: HTMLElement) { document.body.appendChild(node); return { destroy() { node.remove(); } }; }
  function setDialogOpen(next: boolean) { if (!controlledDialog) uncontrolledDialogOpen = next; onOpenChange?.(next); }
  function openDialog() { setDialogOpen(true); }
  function closeDialog() { setDialogOpen(false); queueMicrotask(() => dialogTrigger?.focus()); }
  function dismissDialog(event: KeyboardEvent) { if (event.key !== 'Escape') return; event.preventDefault(); closeDialog(); }
  $effect(() => { if (currentDialogOpen && dialogContent) queueMicrotask(() => dialogContent?.focus()); });
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "Close": Close, "Description": Description, "Dialog": Dialog, "Root": Root, "Title": Title, "Trigger": Trigger, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "open": state_open, "role": state_role, "size": state_size };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  componentState["role"] = componentState["role"];
  emitters.push({ id: "emit-3", event: "change", callback: null, value: () => null });
  layers.add("dialog");
  lifecycles.push({ id: "lifecycle-5", phase: "mounted" });
  focusTargets.add("root");
  refs["root"] ??= undefined;
  styleOperations.push([styles["root"]]);
</script>

<button bind:this={dialogTrigger} type="button" data-kumo-component="Dialog" data-kumo-part="trigger" aria-haspopup="dialog" aria-expanded={currentDialogOpen} class="group flex w-max shrink-0 items-center font-medium select-none border-0 shadow-xs focus:outline-none focus:ring-kumo-focus/50 focus-visible:ring-2 focus-visible:ring-kumo-brand cursor-pointer disabled:cursor-not-allowed disabled:text-kumo-subtle h-9 gap-1.5 rounded-lg px-3 text-base bg-kumo-base !text-kumo-default ring not-disabled:hover:bg-kumo-tint disabled:bg-kumo-base/50 disabled:!text-kumo-default/70 ring-kumo-line data-[state=open]:bg-kumo-base" onclickcapture={openDialog}>{dialogFixture.triggerText ?? triggerText}</button>{#if currentDialogOpen}<div data-base-ui-portal><div role="presentation" style="position:fixed;inset:0;user-select:none"></div><div role="presentation" class="fixed inset-0 bg-kumo-recessed opacity-80 transition-all duration-150 data-ending-style:opacity-0 data-starting-style:opacity-0"></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><div use:portal role="dialog" tabindex="-1" bind:this={dialogContent} class="shadow-xs shadow-m ring ring-kumo-line fixed top-1/2 left-1/2 w-full sm:w-auto max-w-[calc(100vw-2rem)] -translate-x-1/2 -translate-y-1/2 overflow-hidden rounded-xl bg-kumo-base text-kumo-default duration-150 data-ending-style:scale-90 data-ending-style:opacity-0 data-starting-style:scale-90 data-starting-style:opacity-0 sm:min-w-96" style="transition-property:scale,opacity;transition-timing-function:var(--default-transition-timing-function);--tw-shadow:0 20px 25px -5px rgb(0 0 0 / 0.03),0 8px 10px -6px rgb(0 0 0 / 0.03)" onkeydown={dismissDialog}><h2>{dialogFixture.title ?? title}</h2>{#if (dialogFixture.description ?? description) !== undefined}<p>{dialogFixture.description ?? description}</p>{/if}<button type="button" data-kumo-component="Dialog" data-kumo-part="close" class="group flex w-max shrink-0 items-center font-medium select-none border-0 shadow-xs focus:outline-none focus:ring-kumo-focus/50 focus-visible:ring-2 focus-visible:ring-kumo-brand cursor-pointer disabled:cursor-not-allowed disabled:text-kumo-subtle h-9 gap-1.5 rounded-lg px-3 text-base bg-kumo-base !text-kumo-default ring not-disabled:hover:bg-kumo-tint disabled:bg-kumo-base/50 disabled:!text-kumo-default/70 ring-kumo-line data-[state=open]:bg-kumo-base" onclickcapture={closeDialog}>{dialogFixture.closeText ?? closeText}</button></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span></div>{/if}
