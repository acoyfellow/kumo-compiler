<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "f4e0896242a95cf4362bf04ce04ba8f7adcfd1185fdefae81085cb0518b80528";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  className?: unknown;
  onEndDateChange?: unknown;
  onStartDateChange?: unknown;
  size?: unknown;
  timezone?: unknown;
  variant?: unknown;
  "date-range-picker"?: Snippet;
  onEndDateChange?: (value: unknown) => void;
  onStartDateChange?: (value: unknown) => void;
  children?: Snippet;
  onStartChange?: (value: string | null) => void;
  onEndChange?: (value: string | null) => void;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    className = undefined,
    onEndDateChange = undefined,
    onStartDateChange = undefined,
    size = "base",
    timezone = "New York, NY, USA (GMT-4)",
    variant = "default",
    date_range_picker = undefined,
    onStartChange = undefined,
    onEndChange = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_endDate = $state(null);
  let state_months = $state("current and next month");
  let state_startDate = $state(null);
  type DateRangeDay = { iso: string; day: number; inMonth: boolean; className: string; label: string; id: string };
  type DateRangeMonth = { key: string; label: string; days: DateRangeDay[] };
  const DATE_RANGE_MONTH_NAMES = ['January','February','March','April','May','June','July','August','September','October','November','December'];
  const DATE_RANGE_MONTH_ABBR = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
  const DATE_RANGE_WEEKDAY_FULL = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];
  const DATE_RANGE_WEEKDAY_ABBR = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
  const DATE_RANGE_WEEKDAYS = ['Su','Mo','Tu','We','Th','Fr','Sa'];
  const DATE_RANGE_TIMEZONE = 'New York, NY, USA (GMT-4)';
  const DATE_RANGE_DAY_IN = 'h-[26px] w-7 text-sm cursor-pointer text-center transition-all duration-[50] leading-[26px] hover:bg-kumo-interact bg-transparent text-kumo-default';
  const DATE_RANGE_DAY_OUT = 'h-[26px] w-7 text-sm cursor-pointer text-center text-kumo-default transition-all duration-[50] leading-[26px] bg-transparent !text-kumo-subtle';
  const dateRangeMonthNames = DATE_RANGE_MONTH_NAMES;
  const initialDateRangeMonth = (() => { const t = new Date(); return t.getFullYear() + '-' + String(t.getMonth() + 1).padStart(2, '0') + '-01'; })();
  let dateRangeMonth = $state(initialDateRangeMonth);
  let dateRangeStart = $state<string | null>(null);
  let dateRangeEnd = $state<string | null>(null);
  let dateRangeRoot: HTMLElement | undefined = $state();
  function buildDateRangeMonth(value: string): DateRangeMonth { const base = new Date(value + 'T00:00:00.000Z'); const year = base.getUTCFullYear(), month = base.getUTCMonth(); const first = new Date(Date.UTC(year, month, 1)), start = new Date(first); start.setUTCDate(1 - first.getUTCDay()); const days = Array.from({length:42}, (_, index) => { const date = new Date(start); date.setUTCDate(start.getUTCDate() + index); const inMonth = date.getUTCMonth() === month; const wd = date.getUTCDay(); return {iso:date.toISOString().slice(0,10),day:date.getUTCDate(),inMonth,className: inMonth ? DATE_RANGE_DAY_IN : DATE_RANGE_DAY_OUT,label: DATE_RANGE_WEEKDAY_FULL[wd] + ', ' + DATE_RANGE_MONTH_NAMES[date.getUTCMonth()] + ' ' + date.getUTCDate() + ', ' + date.getUTCFullYear(),id: DATE_RANGE_WEEKDAY_ABBR[wd] + ' ' + DATE_RANGE_MONTH_ABBR[date.getUTCMonth()] + ' ' + String(date.getUTCDate()).padStart(2, '0') + ' ' + date.getUTCFullYear()}; }); return {key:year + '-' + month,label:DATE_RANGE_MONTH_NAMES[month] + ' ' + year,days}; }
  const dateRangeMonths = $derived.by(() => [0,1].map(offset => { const date = new Date(dateRangeMonth + 'T00:00:00.000Z'); date.setUTCMonth(date.getUTCMonth() + offset); return buildDateRangeMonth(date.toISOString().slice(0,7) + '-01'); }));
  function moveDateRangeMonth(delta: number) { const date = new Date(dateRangeMonth + 'T00:00:00.000Z'); date.setUTCMonth(date.getUTCMonth() + delta); dateRangeMonth = date.toISOString().slice(0,7) + '-01'; }
  function isDateRangeDayInRange(iso: string) { return Boolean(dateRangeStart && dateRangeEnd && iso >= dateRangeStart && iso <= dateRangeEnd); }
  function selectDateRangeDay(iso: string) { if (dateRangeStart === null || dateRangeEnd !== null || iso < dateRangeStart) { dateRangeStart = iso; dateRangeEnd = null; onStartChange?.(iso); onStartDateChange?.(iso); return; } dateRangeEnd = iso; onEndChange?.(iso); onEndDateChange?.(iso); }
  function resetDateRange() { dateRangeStart = null; dateRangeEnd = null; onStartChange?.(null); onStartDateChange?.(null); onEndChange?.(null); onEndDateChange?.(null); dateRangeRoot?.focus(); }
  function reflectInputValue(node: HTMLInputElement, current: string | undefined) { const reflect = (next: string | undefined) => next === undefined ? node.removeAttribute('value') : node.setAttribute('value', next); reflect(current); return { update: reflect }; }

  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "className": className, "onEndDateChange": onEndDateChange, "onStartDateChange": onStartDateChange, "size": size, "timezone": timezone, "variant": variant, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "endDate": state_endDate, "months": state_months, "startDate": state_startDate };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  componentState["months"] = componentState["months"];
  emitters.push({ id: "emit-3", event: "change", callback: null, value: () => null });
  styleOperations.push([styles["root"]]);
</script>

<div bind:this={dateRangeRoot} class="flex w-fit flex-col rounded-xl select-none bg-kumo-overlay p-4 gap-2.5"><div class="flex gap-4">{#each dateRangeMonths as month, monthIndex (month.key)}<div class="relative w-[196px]">{#if monthIndex === 0}<button type="button" aria-label="Previous month" class="absolute top-0 left-0 cursor-pointer rounded bg-kumo-interact/85 p-1.5 hover:bg-kumo-interact" onclick={() => moveDateRangeMonth(-1)}><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256"><path d="M165.66,202.34a8,8,0,0,1-11.32,11.32l-80-80a8,8,0,0,1,0-11.32l80-80a8,8,0,0,1,11.32,11.32L91.31,128Z"></path></svg></button>{:else}<button type="button" aria-label="Next month" class="absolute top-0 right-0 cursor-pointer rounded bg-kumo-interact/85 p-1.5 hover:bg-kumo-interact" onclick={() => moveDateRangeMonth(1)}><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256"><path d="M181.66,133.66l-80,80a8,8,0,0,1-11.32-11.32L164.69,128,90.34,53.66a8,8,0,0,1,11.32-11.32l80,80A8,8,0,0,1,181.66,133.66Z"></path></svg></button>{/if}<div><div class="mb-3 text-center"><input aria-label="Edit month and year" class="w-full rounded-md border-none bg-transparent py-1.5 text-center font-semibold text-kumo-default transition-all duration-200 focus:outline-none focus:ring-kumo-focus/50 focus:ring-[1.5px] text-sm" value={month.label} use:reflectInputValue={month.label}></div><div class="mt-2 grid grid-cols-7 gap-1">{#each DATE_RANGE_WEEKDAYS as weekday}<div class="h-[22px] text-center text-kumo-subtle w-7 text-sm">{weekday}</div>{/each}</div></div><div class="grid grid-cols-7 gap-0 gap-y-0.5">{#each month.days as day (day.iso)}<button type="button" aria-label={day.label} id={day.id} class={day.className} onclick={() => selectDateRangeDay(day.iso)}>{day.day}</button>{/each}</div></div>{/each}</div><div class="flex items-center gap-2 text-kumo-subtle text-sm"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256"><path d="M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm88,104a87.62,87.62,0,0,1-6.4,32.94l-44.7-27.49a15.92,15.92,0,0,0-6.24-2.23l-22.82-3.08a16.11,16.11,0,0,0-16,7.86h-8.72l-3.8-7.86a15.91,15.91,0,0,0-11-8.67l-8-1.73L96.14,104h16.71a16.06,16.06,0,0,0,7.73-2l12.25-6.76a16.62,16.62,0,0,0,3-2.14l26.91-24.34A15.93,15.93,0,0,0,166,49.1l-.36-.65A88.11,88.11,0,0,1,216,128ZM143.31,41.34,152,56.9,125.09,81.24,112.85,88H96.14a16,16,0,0,0-13.88,8l-8.73,15.23L63.38,84.19,74.32,58.32a87.87,87.87,0,0,1,69-17ZM40,128a87.53,87.53,0,0,1,8.54-37.8l11.34,30.27a16,16,0,0,0,11.62,10l21.43,4.61L96.74,143a16.09,16.09,0,0,0,14.4,9h1.48l-7.23,16.23a16,16,0,0,0,2.86,17.37l.14.14L128,205.94l-1.94,10A88.11,88.11,0,0,1,40,128Zm102.58,86.78,1.13-5.81a16.09,16.09,0,0,0-4-13.9,1.85,1.85,0,0,1-.14-.14L120,174.74,133.7,144l22.82,3.08,45.72,28.12A88.18,88.18,0,0,1,142.58,214.78Z"></path></svg><span class="flex-1">{'Timezone: '}{DATE_RANGE_TIMEZONE}</span><button type="button" class="cursor-pointer font-semibold text-kumo-default underline underline-offset-2" onclick={resetDateRange}>Reset Dates</button></div></div>
