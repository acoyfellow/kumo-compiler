<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "2672fb3450a789e3a7035eb2076aac03fbe9e6e27a3f2df5f37d1b4a9e708c05";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  compound?: Snippet;
  Dialog?: Snippet;
  Input?: Snippet;
  Panel?: Snippet;
  Root?: Snippet;
  collection?: Snippet;
  root?: Snippet;
  children?: Snippet;
  onOpenChange?: (value: boolean) => void;
  onValueChange?: (value: string) => void;
  onHighlightChange?: (value: string) => void;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    compound = undefined,
    Dialog = undefined,
    Input = undefined,
    Panel = undefined,
    Root = undefined,
    collection = undefined,
    root = undefined,
    onOpenChange = undefined,
    onValueChange = undefined,
    onHighlightChange = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_highlight = $state("first result auto-highlighted always and kept as results change");
  let state_modal = $state(true);
  let state_open = $state("required controlled boolean");
  let state_query = $state("controlled value or empty");

  type PaletteFixtureNode = { export?: string; text?: string; props?: Record<string, unknown>; children?: PaletteFixtureNode[] };
  type PaletteFixture = { export?: string; props?: Record<string, unknown>; children?: PaletteFixtureNode[] };
  function paletteText(node: PaletteFixtureNode | undefined): string { return node ? String(node.text ?? '') + (node.children ?? []).map(paletteText).join('') : ''; }
  const paletteFixture = $derived.by(() => { const root = fixture as PaletteFixture | undefined; if (root?.export === '.HighlightedText') return { kind: 'highlighted' as const, text: String(root.props?.text ?? ''), highlights: (root.props?.highlights as Array<[number,number]> | undefined) ?? [], placeholder: undefined, items: [] }; const input = root?.children?.find(node => node.export === '.Input'); const list = root?.children?.find(node => node.export === '.List'); return { kind: 'root' as const, text: '', highlights: [] as Array<[number,number]>, placeholder: input?.props?.placeholder as string | undefined, items: (list?.children ?? []).filter(node => node.export === '.Item').map(node => ({value:String(node.props?.value ?? ''), label:paletteText(node)})), open: Boolean(root?.props?.open), canonicalOverlay: root?.export !== '.Root' }; });
  const paletteSegments = $derived.by(() => { const segments: Array<{text:string;mark:boolean}> = []; let cursor = 0; for (const [start,end] of [...paletteFixture.highlights].sort((a,b)=>a[0]-b[0])) { if (start > cursor) segments.push({text:paletteFixture.text.slice(cursor,start),mark:false}); if (end >= start) segments.push({text:paletteFixture.text.slice(start,end+1),mark:true}); cursor = Math.max(cursor,end+1); } if (cursor < paletteFixture.text.length) segments.push({text:paletteFixture.text.slice(cursor),mark:false}); return segments; });
  let paletteOpen = $state(Boolean((fixture as PaletteFixture | undefined)?.props?.open));
  let paletteValue = $state('');
  let paletteHighlightedIndex = $state(0);
  let paletteInput: HTMLInputElement | undefined = $state();
  let paletteInitialNotified = false;
  $effect(() => { const first = paletteFixture.items[0]; if (browser && paletteFixture.kind === 'root' && paletteOpen && !paletteInitialNotified && first) { paletteInitialNotified = true; onHighlightChange?.(first.value); } });
  function handlePaletteInput(event: Event) { paletteValue = (event.currentTarget as HTMLInputElement).value; onValueChange?.(paletteValue); }
  function handlePaletteKey(event: KeyboardEvent) { if (event.key === 'ArrowDown') { event.preventDefault(); paletteHighlightedIndex = Math.min(paletteHighlightedIndex + 1, paletteFixture.items.length - 1); const item = paletteFixture.items[paletteHighlightedIndex]; if (item) onHighlightChange?.(item.value); return; } if (event.key === 'Escape') { event.preventDefault(); paletteOpen = false; onOpenChange?.(false); queueMicrotask(() => paletteInput?.blur()); } }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "compound": compound, "Dialog": Dialog, "Input": Input, "Panel": Panel, "Root": Root, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "highlight": state_highlight, "modal": state_modal, "open": state_open, "query": state_query };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  componentState["modal"] = componentState["modal"];
  emitters.push({ id: "emit-3", event: "change", callback: null, value: () => null });
  focusTargets.add("root");
  layers.add("command-palette");
  lifecycles.push({ id: "lifecycle-6", phase: "mounted" });
  refs["root"] ??= undefined;
  styleOperations.push([styles["root"]]);
</script>

{#if paletteFixture.kind === 'highlighted'}<span>{#each paletteSegments as segment, index (index)}{#if segment.mark}<mark>{segment.text}</mark>{:else}{segment.text}{/if}{/each}</span>{:else if paletteFixture.canonicalOverlay}<div data-base-ui-portal><div role="presentation" style="position:fixed;inset:0;user-select:none"></div><div role="presentation" class="fixed inset-0 bg-kumo-overlay opacity-80 transition-all duration-150 data-[ending-style]:opacity-0 data-[starting-style]:opacity-0"></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span><div role="dialog" tabindex="-1" class="bg-kumo-base shadow-xs ring ring-kumo-line fixed top-[10vh] left-1/2 w-full max-w-2xl -translate-x-1/2 overflow-hidden rounded-lg duration-150 data-[ending-style]:scale-90 data-[ending-style]:opacity-0 data-[starting-style]:scale-90 data-[starting-style]:opacity-0" style="transition-property:scale,opacity;transition-timing-function:var(--default-transition-timing-function)"><div class="flex max-h-[60vh] flex-col overflow-hidden rounded-lg bg-kumo-elevated"><div class="flex items-center gap-3 bg-kumo-base px-4 py-3 ring-2 ring-kumo-brand focus-within:ring-2 focus-within:ring-kumo-brand" style="--tw-ring-color:var(--color-kumo-brand)"><svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" fill="currentColor" viewBox="0 0 256 256" class="h-4 w-4 text-kumo-subtle"><path d="M232.49,215.51,185,168a92.12,92.12,0,1,0-17,17l47.53,47.54a12,12,0,0,0,17-17ZM44,112a68,68,0,1,1,68,68A68.07,68.07,0,0,1,44,112Z"></path></svg><span role="button" aria-label="Dismiss" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:absolute"></span><input bind:this={paletteInput} role="combobox" aria-expanded="true" aria-haspopup="listbox" aria-autocomplete="list" autocomplete="off" spellcheck="false" placeholder={paletteFixture.placeholder} class="flex-1 border-none bg-transparent text-base kumo-input-placeholder outline-none" value={paletteValue} oninput={handlePaletteInput} onkeydown={handlePaletteKey}></div><div class="relative min-h-0 flex-1 overflow-y-auto rounded-b-lg bg-kumo-base px-2 py-2 scroll-py-2 ring-1 ring-kumo-hairline">{#each paletteFixture.items as item, index (item.value)}<div role="option" class="group flex w-full items-center gap-3 px-2 py-1.5 text-left text-base transition-colors cursor-pointer data-[highlighted]:bg-kumo-overlay rounded-lg" data-highlighted={paletteHighlightedIndex === index ? "" : undefined}>{item.label}</div>{/each}</div><input tabindex="-1" aria-hidden="true" value={paletteValue} style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></div></div><span aria-hidden="true" tabindex="0" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0"></span></div>{:else}<div data-kumo-component="CommandPalette">{#if paletteOpen}<input bind:this={paletteInput} placeholder={paletteFixture.placeholder} value={paletteValue} oninput={handlePaletteInput} onkeydown={handlePaletteKey}><ul role="listbox">{#each paletteFixture.items as item, index (item.value)}<li role="option" data-value={item.value} aria-selected={paletteHighlightedIndex === index}>{item.label}</li>{/each}</ul>{/if}</div>{/if}
