<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "4fe5a01dc4ecdc8eee495f156b879d0413165d27a10f61496537d06c6a4524ca";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  activateOnFocus?: boolean;
  className?: unknown;
  indicatorClassName?: unknown;
  listClassName?: unknown;
  onValueChange?: unknown;
  selectedValue?: unknown;
  size?: unknown;
  tabs?: unknown;
  value?: unknown;
  variant?: unknown;
  collection?: Snippet;
  root?: Snippet;
  onValueChange?: (value: unknown) => void;
  children?: Snippet;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  const TAB_SELECTED_VALUE_OMITTED = Symbol();
  let {
    activateOnFocus = false,
    className = undefined,
    indicatorClassName = undefined,
    listClassName = undefined,
    onValueChange = undefined,
    selectedValue: selectedValueProp = TAB_SELECTED_VALUE_OMITTED,
    size = "base",
    tabs = [],
    value = undefined,
    variant = "segmented",
    collection = undefined,
    root = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_activation = $state("manual unless activateOnFocus is true");
  let state_controlled = $state("value prop");
  let state_overflowing = $state(false);
  let state_uncontrolled = $state("selectedValue or first tab value");

  type TabItem = { label: unknown; value: unknown };
  const tabItems = $derived((tabs ?? []) as TabItem[]);
  const selectedValue = $derived(selectedValueProp === TAB_SELECTED_VALUE_OMITTED ? undefined : selectedValueProp);
  const controlledTab = $derived(selectedValueProp !== TAB_SELECTED_VALUE_OMITTED);
  let uncontrolledTabValue = $state(tabItems[0]?.value);
  const selectedTabValue = $derived(controlledTab ? selectedValue : uncontrolledTabValue);
  let focusedIndex = $state(0);
  let tabButtons: HTMLButtonElement[] = $state([]);
  $effect(() => { const index = tabItems.findIndex(item => item.value === selectedTabValue); if (index >= 0) focusedIndex = index; });
  function commitTab(index: number) { const item = tabItems[index]; if (!item) return; focusedIndex = index; if (!controlledTab) uncontrolledTabValue = item.value; onValueChange?.(item.value); tabButtons[index]?.focus(); }
  function handleTabKey(event: KeyboardEvent, index: number) { if (event.key === 'ArrowRight') { event.preventDefault(); const next = Math.min(index + 1, tabItems.length - 1); focusedIndex = next; tabButtons[next]?.focus(); if (activateOnFocus) commitTab(next); return; } if (event.key === 'Enter' || event.key === ' ' || event.code === 'Space') { event.preventDefault(); commitTab(index); } }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "activateOnFocus": activateOnFocus, "className": className, "indicatorClassName": indicatorClassName, "listClassName": listClassName, "onValueChange": onValueChange, "selectedValue": selectedValue, "size": size, "tabs": tabs, "value": value, "variant": variant, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "activation": state_activation, "controlled": state_controlled, "overflowing": state_overflowing, "uncontrolled": state_uncontrolled };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const groupOf = (t: string) => /^w-/.test(t) ? 'w' : /^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t) ? 'text-size' : null; const last: Record<string, number> = {}; tokens.forEach((t, i) => { const g = groupOf(t); if (g) last[g] = i; }); return tokens.filter((t, i) => { const g = groupOf(t); return !g || last[g] === i; }).join(' '); };
  void "render-1";
  componentState["controlled"] = componentState["controlled"];
  emitters.push({ id: "emit-3", event: "change", callback: null, value: () => null });
  focusTargets.add("root");
  refs["root"] ??= undefined;
  styleOperations.push([styles["root"]]);
</script>

<div data-orientation="horizontal" data-activation-direction="none" class="relative isolate min-w-0 font-medium rounded-lg ring ring-kumo-hairline/70"><div class="absolute inset-x-0 top-1/2 z-0 -translate-y-1/2 rounded-lg bg-kumo-recessed h-9"></div><div data-orientation="horizontal" data-activation-direction="none" role="tablist" class="relative flex min-w-0 shrink items-stretch kumo-tabs-list overflow-x-auto rounded-lg bg-kumo-recessed px-0.5 [--scroll-fade-width:3rem] scroll-px-(--scroll-fade-width) h-9">{#each tabItems as item, index (item.value)}<button bind:this={tabButtons[index]} type="button" data-orientation="horizontal" aria-disabled="false" data-kumo-component="Tabs" data-kumo-part="tab" role="tab" class="relative z-2 flex items-center bg-transparent whitespace-nowrap focus:outline-none focus:ring-kumo-focus/50 focus-visible:ring-2 focus-visible:ring-kumo-brand cursor-pointer text-base my-0.5 text-kumo-subtle hover:text-kumo-default aria-selected:text-kumo-default focus-visible:ring-inset px-2.5 rounded-md" data-active={selectedTabValue === item.value ? "" : undefined} tabindex={focusedIndex === index ? 0 : -1} aria-selected={selectedTabValue === item.value} onclickcapture={() => commitTab(index)} onkeydowncapture={(event) => handleTabKey(event, index)}>{item.label}</button>{/each}<div data-orientation="horizontal" data-activation-direction="none" role="presentation" hidden class="absolute z-1 left-0 w-(--active-tab-width) translate-x-(--active-tab-left) transition-all duration-200 data-[rendered=false]:scale-90 data-[rendered=false]:opacity-0 top-(--active-tab-top) h-(--active-tab-height) bg-kumo-base shadow-sm ring ring-kumo-line rounded-md"></div></div></div>
