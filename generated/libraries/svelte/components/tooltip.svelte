<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
  import { setContext } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "df0f1bd6e7c3df82a5312f35a777e1939a90560a9ea2664670f0bcdfbbe85d2d";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  content?: unknown;
  children?: Snippet;
  open?: boolean;
  defaultOpen?: boolean;
  asChild?: boolean;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    content = undefined,
    open = undefined,
    defaultOpen = undefined,
    asChild = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  

  const tooltipOpen = $derived(open !== undefined ? Boolean(open) : Boolean(defaultOpen));
  const tooltipContent = $derived(typeof content === 'string' || typeof content === 'number' ? String(content) : fixtureText(content));
  let tooltipAnchor: HTMLElement | undefined = $state();
  function tooltipPortal(node: HTMLElement) {
    document.body.appendChild(node);
    // Position the portaled popup at the trigger (top/center) the way golden's
    // floating-ui does: read the trigger rect and set the positioner transform +
    // the floating-ui CSS vars golden exposes, instead of a hardcoded offset.
    const position = () => {
      const trigger = (tooltipAnchor?.matches('[data-base-ui-tooltip-trigger]') ? tooltipAnchor : tooltipAnchor?.querySelector('[data-base-ui-tooltip-trigger]')) as HTMLElement | null;
      const positioner = node.querySelector('[role="presentation"]') as HTMLElement | null;
      const popup = node.querySelector('[data-base-ui-focusable]') as HTMLElement | null;
      if (!trigger || !positioner || !popup) return;
      const t = trigger.getBoundingClientRect(); const pop = popup.getBoundingClientRect();
      const left = Math.round((t.left + t.width / 2 - pop.width / 2) * 100) / 100;
      const top = Math.round((t.top - pop.height - 10) * 100) / 100;
      positioner.style.transform = 'translate(' + left + 'px,' + top + 'px)';
      positioner.style.setProperty('--available-width', (document.documentElement.clientWidth - 10) + 'px');
      positioner.style.setProperty('--available-height', (document.documentElement.clientHeight - 11) + 'px');
    };
    requestAnimationFrame(position);
    return { destroy() { node.remove(); } };
  }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "content": content, "open": open, "defaultOpen": defaultOpen, "asChild": asChild, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  setContext('kumo.merge-trigger', semanticValues.asChild ? { 'data-base-ui-tooltip-trigger': '' } : undefined);
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = {  };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  styleOperations.push([styles["root"]]);
</script>

<div style="display:contents" bind:this={tooltipAnchor}>{#if asChild}{#if children}{@render children()}{/if}{:else}<button class="inline-flex items-center bg-transparent border-none shadow-none p-0 m-0 h-auto min-h-0 leading-[0] cursor-default" type="button" data-base-ui-tooltip-trigger="">{#if children}{@render children()}{/if}</button>{/if}</div>{#if browser && tooltipOpen}<div use:tooltipPortal data-base-ui-portal data-base-ui-inert><div data-open="" data-side="top" data-align="center" role="presentation" class="max-w-[var(--available-width)]" style="position:absolute;left:0;top:0;transform:translate(0px,-10px)"><div data-open="" data-side="top" data-align="center" tabindex="-1" data-base-ui-focusable="" class="flex origin-[var(--transform-origin)] flex-col rounded-md bg-kumo-base px-2.5 py-1.5 text-sm text-kumo-default shadow-lg shadow-kumo-tip-shadow outline outline-kumo-fill transition-[transform,scale,opacity] duration-150 data-[starting-style]:scale-90 data-[starting-style]:opacity-0 data-[ending-style]:scale-90 data-[ending-style]:opacity-0 data-[instant]:duration-0 kumo-tooltip-popup"><div data-open="" data-side="top" data-align="center" aria-hidden="true" class="flex data-[side=bottom]:top-[-8px] data-[side=left]:right-[-13px] data-[side=left]:rotate-90 data-[side=right]:left-[-13px] data-[side=right]:-rotate-90 data-[side=top]:bottom-[-8px] data-[side=top]:rotate-180" style="position:absolute;left:12px"><svg width="20" height="10" viewBox="0 0 20 10" fill="none"><path d="M9.66437 2.60207L4.80758 6.97318C4.07308 7.63423 3.11989 8 2.13172 8H0V10H20V8H18.5349C17.5468 8 16.5936 7.63423 15.8591 6.97318L11.0023 2.60207C10.622 2.2598 10.0447 2.25979 9.66437 2.60207Z" class="fill-kumo-base"></path><path d="M8.99542 1.85876C9.75604 1.17425 10.9106 1.17422 11.6713 1.85878L16.5281 6.22989C17.0789 6.72568 17.7938 7.00001 18.5349 7.00001L15.89 7L11.0023 2.60207C10.622 2.2598 10.0447 2.2598 9.66436 2.60207L4.77734 7L2.13171 7.00001C2.87284 7.00001 3.58774 6.72568 4.13861 6.22989L8.99542 1.85876Z" class="fill-kumo-tip-shadow"></path><path d="M10.3333 3.34539L5.47654 7.71648C4.55842 8.54279 3.36693 9 2.13172 9H0V8H2.13172C3.11989 8 4.07308 7.63423 4.80758 6.97318L9.66437 2.60207C10.0447 2.25979 10.622 2.2598 11.0023 2.60207L15.8591 6.97318C16.5936 7.63423 17.5468 8 18.5349 8H20V9H18.5349C17.2998 9 16.1083 8.54278 15.1901 7.71648L10.3333 3.34539Z" class="fill-kumo-tip-stroke"></path></svg></div>{tooltipContent}</div></div></div>{/if}
