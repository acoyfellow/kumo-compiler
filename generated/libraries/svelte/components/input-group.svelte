<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "6fe3c258c7b23c525441c42e3f981c41f5efd822cc7239b5d8fc28ba519b12a7";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  observable?: unknown;
  children?: Snippet;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    observable = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_source = $state("props/native state");

  type InputGroupFixtureNode = { export?: string; text?: string; props?: Record<string, unknown>; children?: InputGroupFixtureNode[] };
  type InputGroupFixture = { props?: { label?: unknown; description?: unknown; required?: boolean }; children?: InputGroupFixtureNode[] };
  const inputGroupFixture = $derived.by(() => { const root = fixture as InputGroupFixture | undefined; return { label: root?.props?.label, description: root?.props?.description, required: root?.props?.required, children: root?.children ?? [] }; });
  const inputGroupId = "kumo-32ed572874d2";
  let inputGroupValue = $state('');
  function inputGroupText(node: InputGroupFixtureNode | undefined): string { return node ? String(node.text ?? '') + (node.children ?? []).map(inputGroupText).join('') : ''; }
  function handleInputGroupInput(event: Event) { inputGroupValue = (event.currentTarget as HTMLInputElement).value; }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "observable": observable, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "source": state_source };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  emitters.push({ id: "emit-2", event: "change", callback: null, value: () => null });
  refs["root"] ??= undefined;
  styleOperations.push([styles["root"]]);
</script>

<label {...rest} data-slot="input-group" data-focus-mode="container" class="relative w-full cursor-text border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled h-9 rounded-lg text-base focus:ring-kumo-focus/50 focus:ring-[1.5px] shadow-xs data-[disabled]:pointer-events-none data-[disabled]:opacity-50 overflow-hidden focus-within:ring-kumo-focus/50 focus-within:ring-[1.5px] has-[input[aria-invalid=true]]:ring-kumo-danger px-0 flex items-center gap-0 has-[[data-slot=input-group-suffix]]:[&_input]:[field-sizing:content] has-[[data-slot=input-group-suffix]]:[&_input]:max-w-full has-[[data-slot=input-group-suffix]]:[&_input]:grow-0 has-[[data-slot=input-group-suffix]]:[&_input]:pr-0 has-[[data-slot=input-group-addon-start]]:[&_input]:pl-2 has-[[data-slot=input-group-addon-end]]:[&_input]:pr-2 mb-0!">{#if inputGroupFixture.children.length}{#if inputGroupFixture.label !== undefined}<span data-for={inputGroupId}>{inputGroupFixture.label}</span>{/if}{#if inputGroupFixture.description !== undefined}<span>{inputGroupFixture.description}</span>{/if}{#each inputGroupFixture.children as part, index (part.export ?? index)}{#if part.export === '.Addon'}<span data-slot="input-group-addon-start">{inputGroupText(part)}</span>{:else if part.export === '.Input'}<input id={inputGroupId} aria-label={part.props?.['aria-label'] as string | undefined} disabled={Boolean(part.props?.disabled)} required={inputGroupFixture.required as boolean | undefined} value={inputGroupValue} oninput={handleInputGroupInput}>{:else if part.export === '.Button'}<button type="button" data-variant={part.props?.variant as string | undefined}>{inputGroupText(part)}</button>{:else if part.export === '.Suffix'}<span data-slot="input-group-suffix">{inputGroupText(part)}</span>{/if}{/each}{:else}{#if children}{@render children()}{/if}{/if}</label>
