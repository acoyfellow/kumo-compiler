<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "0fb1bd718d40fbc0aecffd04f5e7e67ffb54bd77534c52ac02ff74e9f52dedc1";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  defaultValue?: unknown;
  disabled?: boolean;
  items?: unknown;
  onValueChange?: unknown;
  orientation?: unknown;
  value?: unknown;
  collection?: Snippet;
  root?: Snippet;
  onValueChange?: (value: unknown) => void;
  children?: Snippet;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    defaultValue = undefined,
    disabled = undefined,
    items = undefined,
    onValueChange = undefined,
    orientation = "vertical",
    value = undefined,
    collection = undefined,
    root = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_controlled = $state("value");
  let state_uncontrolled = $state("defaultValue");

  type RadioItem = { label: string; value: unknown; disabled?: boolean };
   type RadioFixture = { kind?: 'radio-group'; legend?: string; items?: RadioItem[]; defaultValue?: unknown; value?: unknown; disabled?: boolean };
   const radioFixture = $derived((fixture ?? {}) as RadioFixture);
   const radioItems = $derived(radioFixture.items ?? []);
   const controlledRadio = $derived(Object.prototype.hasOwnProperty.call(radioFixture, 'value'));
   let uncontrolledRadioValue = $state((fixture as RadioFixture | undefined)?.defaultValue);
   const selectedRadioValue = $derived(controlledRadio ? radioFixture.value : uncontrolledRadioValue);
   const radioIdBase = $props.id();
   const radioLegendId = radioIdBase + '-legend';
   const radioItemId = (index: number) => radioIdBase + '-item-' + index;
   const radioInputId = (index: number) => radioIdBase + '-input-' + index;
   const radioLabelId = (index: number) => radioInputId(index) + '-label';
   function reflectRadioChecked(node: HTMLInputElement, current: boolean) { const reflect = (next: boolean) => next ? node.setAttribute('checked', '') : node.removeAttribute('checked'); reflect(current); return { update: reflect }; }
   let radioRoot: HTMLElement | undefined = $state();
   function selectRadio(item: RadioItem) { if (radioFixture.disabled || item.disabled) return; if (!controlledRadio) uncontrolledRadioValue = item.value; onValueChange?.(item.value); if (radioRoot) { radioRoot.setAttribute('tabindex', '-1'); radioRoot.focus(); } }
   function selectNextRadio(event: KeyboardEvent, index: number) { if (event.key !== 'ArrowDown' || radioFixture.disabled) return; event.preventDefault(); const items = radioItems; for (let offset = 1; offset <= items.length; offset++) { const next = items[(index + offset) % items.length]; if (!next.disabled) { selectRadio(next); return; } } }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "defaultValue": defaultValue, "disabled": disabled, "items": items, "onValueChange": onValueChange, "orientation": orientation, "value": value, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "controlled": state_controlled, "uncontrolled": state_uncontrolled };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  componentState["controlled"] = componentState["controlled"];
  emitters.push({ id: "emit-2", event: "change", callback: null, value: () => null });
  void "render-3";
  focusTargets.add("root");
  refs["root"] ??= undefined;
  styleOperations.push([styles["root"]]);
</script>

<div bind:this={radioRoot} role="radiogroup"><fieldset class="flex flex-col gap-4" aria-labelledby={radioFixture.legend ? radioLegendId : undefined}>{#if radioFixture.legend}<div id={radioLegendId} class="text-base font-medium text-kumo-default">{radioFixture.legend}</div>{/if}<div class="flex flex-col gap-2">{#each radioItems as item, index (item.value)}<label id={radioLabelId(index)} data-kumo-component="Radio" data-kumo-part="item-label" class="m-0 group relative inline-flex items-start gap-2 cursor-pointer"><span id={radioItemId(index)} data-kumo-component="Radio" data-kumo-part="item" class="relative mt-0.5 flex h-4 w-4 shrink-0 items-center justify-center rounded-full border-0 bg-kumo-base ring focus:outline-none after:absolute after:-inset-x-3 after:-inset-y-2 ring-kumo-line group-hover:ring-kumo-hairline focus:ring-kumo-focus focus:ring-2 focus-visible:ring-2 focus-visible:ring-kumo-brand focus-visible:outline-offset-3 data-[checked]:bg-kumo-contrast" role="radio" tabindex={selectedRadioValue === item.value && !(radioFixture.disabled || item.disabled) ? 0 : -1} aria-checked={selectedRadioValue === item.value} aria-labelledby={radioLabelId(index)} aria-disabled={Boolean(radioFixture.disabled || item.disabled) || undefined} data-composite-item-active={selectedRadioValue === item.value ? "" : undefined} data-checked={selectedRadioValue === item.value ? "" : undefined} data-unchecked={selectedRadioValue === item.value ? undefined : ""} onclickcapture={(event) => { event.preventDefault(); selectRadio(item); }} onkeydowncapture={(event) => selectNextRadio(event, index)}><span class="flex items-center justify-center" data-checked={selectedRadioValue === item.value ? "" : undefined} data-unchecked={selectedRadioValue === item.value ? undefined : ""}><span class="h-2 w-2 rounded-full bg-kumo-base"></span></span></span><input id={radioInputId(index)} type="radio" tabindex="-1" aria-hidden="true" value={String(item.value)} checked={selectedRadioValue === item.value} use:reflectRadioChecked={selectedRadioValue === item.value} disabled={Boolean(radioFixture.disabled || item.disabled) || undefined} style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0" onchange={() => { if (selectedRadioValue !== item.value) selectRadio(item); }}><span class="text-base text-kumo-default">{item.label}</span></label>{/each}</div></fieldset></div>
