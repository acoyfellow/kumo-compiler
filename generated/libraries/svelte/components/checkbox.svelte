<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "d9617fd05509e055a82540aec66516f53dc6bd6a3761ab62cda5c32bdac39119";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  checked?: boolean;
  disabled?: boolean;
  group?: unknown;
  indeterminate?: boolean;
  label?: unknown;
  onCheckedChange?: unknown;
  onCheckedChange?: (value: unknown) => void;
  children?: Snippet;
  defaultChecked?: boolean;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    checked = undefined,
    disabled = false,
    group = undefined,
    indeterminate = false,
    label = undefined,
    onCheckedChange = undefined,
    defaultChecked = false,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_controlled = $state("checked");
  let state_group = $state("defaultValue/value");
  let state_uncontrolled = $state("absence of checked");
  const controlledToggle = checked !== undefined;
  let uncontrolledChecked = $state(Boolean(defaultChecked));
  const currentChecked = $derived(controlledToggle ? Boolean(checked) : uncontrolledChecked);
  let currentIndeterminate = $state(Boolean(indeterminate));
  function activateToggle() {
    if (disabled) return;
    const next = currentIndeterminate ? true : !currentChecked;
    currentIndeterminate = false;
    if (!controlledToggle) uncontrolledChecked = next;
    onCheckedChange?.(next);
  }
  function activateToggleOnSpace(event: KeyboardEvent) { if (event.code === 'Space' || event.key === ' ') { event.preventDefault(); activateToggle(); } }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "checked": checked, "disabled": disabled, "group": group, "indeterminate": indeterminate, "label": label, "onCheckedChange": onCheckedChange, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "controlled": state_controlled, "group": state_group, "uncontrolled": state_uncontrolled };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => values.filter(Boolean).join(' ');
  componentState["controlled"] = componentState["controlled"];
  emitters.push({ id: "emit-2", event: "change", callback: null, value: () => null });
  void "render-3";
  styleOperations.push([styles["root"]]);
</script>

<span class={"relative flex h-4 w-4 shrink-0 items-center justify-center rounded-sm border-0 bg-kumo-base ring ring-kumo-hairline hover:ring-kumo-hairline focus:outline-none focus:ring-kumo-focus focus:ring-2 focus-visible:ring-2 focus-visible:ring-kumo-brand data-[checked]:bg-kumo-contrast data-[checked]:ring-kumo-contrast data-[indeterminate]:bg-kumo-contrast data-[indeterminate]:ring-kumo-contrast text-kumo-inverse"} data-state={(currentIndeterminate) ? "indeterminate" : (currentChecked ? "checked" : "unchecked")} data-checked={currentChecked ? "" : undefined} data-unchecked={currentChecked ? undefined : ""} data-indeterminate={(currentIndeterminate) ? "" : undefined} aria-label={rest["aria-label"] as string | undefined} role="checkbox" aria-checked={currentIndeterminate ? "mixed" : currentChecked} aria-disabled={disabled || undefined} tabindex={disabled ? undefined : 0} onclickcapture={activateToggle} onkeydowncapture={activateToggleOnSpace}>{#if label}{label}{/if}</span>
