<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "f5cda68ad861d0f3ad20ee73a5e65677951bd72052b34e2745bbf65c2682bdd3";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  className?: unknown;
  customValue?: unknown;
  indicatorClassName?: unknown;
  label: unknown;
  max?: number;
  min?: number;
  showValue?: boolean;
  trackClassName?: unknown;
  value?: number;
  children?: Snippet;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    className = undefined,
    customValue = undefined,
    indicatorClassName = undefined,
    label = undefined,
    max = 100,
    min = 0,
    showValue = true,
    trackClassName = undefined,
    value = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  

  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "className": className, "customValue": customValue, "indicatorClassName": indicatorClassName, "label": label, "max": max, "min": min, "showValue": showValue, "trackClassName": trackClassName, "value": value, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = {  };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const keysOf = (t: string): string[] => { const k: string[] = []; if (/^w-/.test(t)) k.push('w'); if (/^h-/.test(t)) k.push('h'); if (/^size-/.test(t)) k.push('w','h'); if (/^p-/.test(t)) k.push('p','px','py','pt','pb','pl','pr'); if (/^px-/.test(t)) k.push('px','pl','pr'); if (/^py-/.test(t)) k.push('py','pt','pb'); if (/^pt-/.test(t)) k.push('pt'); if (/^pb-/.test(t)) k.push('pb'); if (/^pl-/.test(t)) k.push('pl'); if (/^pr-/.test(t)) k.push('pr'); if (/^rounded(-|$)/.test(t)) k.push('rounded'); if (/^gap-/.test(t)) k.push('gap'); if (/^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t)) k.push('text-size'); if (/^justify-/.test(t)) k.push('justify'); return k; }; const keys = tokens.map(keysOf); return tokens.filter((t, i) => { const ks = keys[i]; if (!ks.length) return true; for (let j = i + 1; j < tokens.length; j++) if (keys[j].some(k => ks.includes(k))) return false; return true; }).join(' '); };
  void "render-1";
  styleOperations.push([styles["root"]]);
</script>

<div class="flex w-full flex-col gap-2" role="meter" aria-valuenow={value} aria-valuemin={min ?? 0} aria-valuemax={max ?? 100} aria-valuetext={Math.round(((value ?? 0) - (min ?? 0)) / (((max ?? 100) - (min ?? 0)) || 1) * 100) + "%"} aria-labelledby="kumo-703aed29b63a-meter-label"><div class="flex items-center justify-between gap-4"><span id="kumo-703aed29b63a-meter-label" role="presentation" class="text-xs text-kumo-subtle">{label}</span>{#if showValue !== false}<span class="text-sm font-medium text-kumo-default tabular-nums">{customValue ?? (value + "%")}</span>{/if}</div><div class="relative h-2 w-full overflow-hidden rounded-full bg-kumo-fill"><div class="absolute inset-y-0 left-0 rounded-full bg-linear-to-r from-kumo-brand via-kumo-brand to-kumo-brand transition-[width] duration-300 ease-out" style="inset-inline-start:0;height:inherit;width:{value}%"></div></div><span role="presentation" style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0">x</span></div>
