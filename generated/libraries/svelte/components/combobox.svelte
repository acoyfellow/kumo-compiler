<!-- @generated by src/kumo/emitters/svelte/index.mjs; do not edit -->
<script lang="ts">
  import type { Snippet } from 'svelte';
   const browser = typeof document !== 'undefined';

  export const modelDigest = "87e32915ebbd07b300ba0f9ea11293bed3a0e1d43b56400838cf3b0b3064be90";
  export const contentBindingDigest = "a6655036dbbdb2cd56a9e62bf5f2f8f75bb6a7bb4d3c5fbf41726fd8666277cd";
  export type Props = {
  compound?: Snippet;
  Content?: Snippet;
  root?: Snippet;
  TriggerInput?: Snippet;
  TriggerMultipleWithInput?: Snippet;
  variants?: unknown;
  collection?: Snippet;
  children?: Snippet;
  onOpenChange?: (value: boolean) => void;
  onValueChange?: (value: string) => void;
  styles?: Record<string, string>;
  fixture?: unknown;
  [key: string]: unknown;
};

  let {
    compound = undefined,
    Content = undefined,
    root = undefined,
    TriggerInput = undefined,
    TriggerMultipleWithInput = undefined,
    variants = undefined,
    collection = undefined,
    onOpenChange = undefined,
    onValueChange = undefined,
    children,
    fixture = undefined,
    __consumerContent = undefined,
    styles = {},
    ...rest
  }: Props = $props();
  let state_inputSide = $state("right");
  let state_multiple = $state("false unless set");
  let state_open = $state("closed unless controlled/default-open");
  let state_selection = $state("value/defaultValue according to Base UI controlled mode");

  type ComboboxFixtureNode = { export?: string; text?: string; props?: Record<string, unknown>; children?: ComboboxFixtureNode[] };
  type ComboboxFixture = { children?: ComboboxFixtureNode[] };
  function comboboxText(node: ComboboxFixtureNode | undefined): string { return node ? String(node.text ?? '') + (node.children ?? []).map(comboboxText).join('') : ''; }
  const comboboxFixture = $derived.by(() => { const root = fixture as ComboboxFixture | undefined; const trigger = root?.children?.find(node => node.export === '.TriggerInput'); const content = root?.children?.find(node => node.export === '.Content'); const list = content?.children?.find(node => node.export === '.List'); return { placeholder: trigger?.props?.placeholder as string | undefined, items: (list?.children ?? []).filter(node => node.export === '.Item').map(node => ({ value: String(node.props?.value ?? ''), label: comboboxText(node) })) }; });
  let comboboxOpen = $state(false);
  let highlightedIndex = $state(-1);
  let comboboxValue = $state('');
  let comboboxInput: HTMLInputElement | undefined = $state();
  function reflectComboboxValue(node: HTMLInputElement, current: string) { node.setAttribute('value', current); return { update: (next: string) => node.setAttribute('value', next) }; }
  const filteredComboboxItems = $derived.by(() => { const query = comboboxValue.trim().toLocaleLowerCase(); return query ? comboboxFixture.items.filter(item => item.label.toLocaleLowerCase().includes(query)) : comboboxFixture.items; });
  function setComboboxOpen(next: boolean) { if (comboboxOpen === next) return; comboboxOpen = next; onOpenChange?.(next); }
  function openCombobox() { setComboboxOpen(true); }
  function handleComboboxInput(event: Event) { comboboxValue = (event.currentTarget as HTMLInputElement).value; highlightedIndex = -1; openCombobox(); }
  function commitComboboxItem(item: { value: string; label: string }) { comboboxValue = item.value; onValueChange?.(item.value); setComboboxOpen(false); comboboxInput?.focus(); }
  function handleComboboxKey(event: KeyboardEvent) { if (event.key === 'ArrowDown') { event.preventDefault(); openCombobox(); highlightedIndex = Math.min(highlightedIndex + 1, filteredComboboxItems.length - 1); return; } if (event.key === 'Enter' && highlightedIndex >= 0) { event.preventDefault(); const item = filteredComboboxItems[highlightedIndex]; if (item) commitComboboxItem(item); } }
  const renderContent = __consumerContent;
  const semanticProps: Record<string, unknown> = { "compound": compound, "Content": Content, "root": root, "TriggerInput": TriggerInput, "TriggerMultipleWithInput": TriggerMultipleWithInput, "variants": variants, ...rest, ...(__consumerContent !== undefined ? {children: renderContent} : {}) };
  const semanticValues = semanticProps;
  const semanticEqual = (left: unknown, right: unknown) => JSON.stringify(left) === JSON.stringify(right);
  const fixtureText = (value: any): string => value && typeof value === 'object' ? String(typeof value.text === 'string' ? value.text : '') + (Array.isArray(value.children) ? value.children.map(fixtureText).join('') : '') : '';
  const componentState: Record<string, unknown> = { "inputSide": state_inputSide, "multiple": state_multiple, "open": state_open, "selection": state_selection };
  const refs: Record<string, HTMLElement | undefined> = {};
  const emitters: Array<{id:string,event:string,callback:string|null,value:()=>unknown}> = [];
  const focusTargets = new Set<string>();
  const lifecycles: Array<{id:string,phase:string}> = [];
  const services = new Set<string>();
  const layers = new Set<string>();
  const styleOperations: unknown[][] = [];
  const cx = (...values: unknown[]) => { const tokens = values.filter(Boolean).join(' ').split(/\s+/).filter(Boolean); const groupOf = (t: string) => /^w-/.test(t) ? 'w' : /^text-(xs|sm|base|lg|xl|2xl|3xl|4xl|5xl|6xl|7xl|8xl|9xl)$/.test(t) ? 'text-size' : null; const last: Record<string, number> = {}; tokens.forEach((t, i) => { const g = groupOf(t); if (g) last[g] = i; }); return tokens.filter((t, i) => { const g = groupOf(t); return !g || last[g] === i; }).join(' '); };
  void "render-1";
  componentState["multiple"] = componentState["multiple"];
  emitters.push({ id: "emit-3", event: "change", callback: null, value: () => null });
  refs["root"] ??= undefined;
  focusTargets.add("root");
  styleOperations.push([styles["root"]]);
</script>

{#if !comboboxOpen && rest.open === undefined && rest.defaultOpen === undefined}<div class="relative inline-block w-full max-w-xs has-[:disabled]:opacity-50 has-[:disabled]:cursor-not-allowed"><input bind:this={comboboxInput} {...rest} autocomplete="off" spellcheck="false" autocorrect="off" autocapitalize="none" role="combobox" aria-expanded="false" aria-haspopup="listbox" aria-autocomplete="list" type="text" placeholder={comboboxFixture.placeholder} class="border-0 bg-kumo-control text-kumo-default ring ring-kumo-line outline-none focus:outline-none kumo-input-placeholder disabled:text-kumo-disabled h-9 gap-1.5 rounded-lg px-3 text-base focus:ring-kumo-focus/50 focus:ring-[1.5px] w-full pr-12 disabled:cursor-not-allowed" value={comboboxValue} use:reflectComboboxValue={comboboxValue} onclick={openCombobox} onfocus={openCombobox} oninput={handleComboboxInput} onkeydown={handleComboboxKey}><button type="button" data-placeholder="" tabindex="0" aria-expanded="false" aria-haspopup="dialog" data-kumo-component="Combobox" data-kumo-part="trigger" aria-label="Show options" class="absolute top-1/2 -translate-y-1/2 flex items-center justify-center cursor-pointer text-kumo-subtle m-0 bg-transparent p-0 right-2" onclick={openCombobox} onkeydown={handleComboboxKey}><span aria-hidden="true" class="flex items-center"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 256 256" class="fill-current"><path d="M213.66,101.66l-80,80a8,8,0,0,1-11.32,0l-80-80A8,8,0,0,1,53.66,90.34L128,164.69l74.34-74.35a8,8,0,0,1,11.32,11.32Z"></path></svg></span></button></div><input style="clip-path:inset(50%);overflow:hidden;white-space:nowrap;border:0;padding:0;width:1px;height:1px;margin:-1px;position:fixed;top:0;left:0" tabindex="-1" aria-hidden="true" value={comboboxValue} use:reflectComboboxValue={comboboxValue}>{:else}<input bind:this={comboboxInput} role="combobox" aria-expanded={comboboxOpen} placeholder={comboboxFixture.placeholder} value={comboboxValue} onclick={openCombobox} onfocus={openCombobox} oninput={handleComboboxInput} onkeydown={handleComboboxKey}>{#if comboboxOpen}<ul role="listbox">{#each filteredComboboxItems as item, index (item.value)}<li role="option" data-value={item.value} aria-selected={highlightedIndex === index} onpointerdown={(event) => event.preventDefault()} onclick={() => commitComboboxItem(item)}>{item.label}</li>{/each}</ul>{/if}{/if}
