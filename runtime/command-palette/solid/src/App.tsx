import {For,Show,createMemo,createSignal} from 'solid-js';const options=["Create project","Open settings"] as const;export default function CommandPalette(){const [query,setQuery]=createSignal(''),[open,setOpen]=createSignal(false),[active,setActive]=createSignal(0);const filtered=createMemo(()=>options.filter(x=>x.toLowerCase().includes(query().toLowerCase())));const choose=()=>{const x=filtered()[active()];if(x)setQuery(x);setOpen(false)};const key=(e:KeyboardEvent)=>{if(e.key==='Escape')setOpen(false);else if(e.key==='ArrowDown'){e.preventDefault();setOpen(true);setActive(i=>Math.min(filtered().length-1,i+1))}else if(e.key==='ArrowUp'){e.preventDefault();setActive(i=>Math.max(0,i-1))}else if(e.key==='Enter'){e.preventDefault();choose()}};return <main class="selection-shell"><h1>CommandPalette</h1><label for="selection-query">Command</label><input id="selection-query" role="combobox" aria-autocomplete="list" aria-expanded={open()} aria-controls="selection-list" aria-activedescendant={open()?'selection-'+active():undefined} value={query()} onFocus={()=>setOpen(true)} onInput={e=>{setQuery(e.currentTarget.value);setActive(0);setOpen(true)}} onKeyDown={key}/><Show when={open()}><ul id="selection-list" role="listbox"><For each={filtered()}>{(o,i)=><li id={'selection-'+i()} role="option" aria-selected={active()===i()} onPointerDown={()=>{setActive(i());choose()}}>{o}</li>}</For></ul></Show></main>}
