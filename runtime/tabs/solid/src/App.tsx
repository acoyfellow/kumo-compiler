import {For,createSignal} from 'solid-js';
const labels=["Overview","Activity","Settings"] as const;
export default function Tabs(){const [active,setActive]=createSignal(0);let items:HTMLButtonElement[]=[];const move=(event:KeyboardEvent,index:number)=>{let next=index;if(event.key==='ArrowRight')next=(index+1)%labels.length;else if(event.key==='ArrowLeft')next=(index-1+labels.length)%labels.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=labels.length-1;else return;event.preventDefault();setActive(next);items[next]?.focus()};return <main class="nav-shell"><h1>Tabs</h1><div class="tabs" role="tablist" aria-label="Account"><For each={labels}>{(label,index)=><button ref={el=>items[index()]=el} role="tab" tabindex={active()===index()?0:-1} aria-selected={active()===index()} aria-controls={`panel-${index()}`} id={`tab-${index()}`} onClick={()=>{setActive(index());items[index()]?.focus()}} onKeyDown={event=>move(event,index())}>{label}</button>}</For></div><div id={`panel-${active()}`} class="panel" role="tabpanel" aria-labelledby={`tab-${active()}`} tabindex="0">{labels[active()]} content</div></main>}
