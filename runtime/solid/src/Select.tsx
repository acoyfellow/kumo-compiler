import {createSignal} from 'solid-js'; export default function Select(p){const [open,setOpen]=createSignal(false),[selected,setSelected]=createSignal(p.value||'');const id=()=> (p.label||'select').toLowerCase().replace(/[^a-z0-9]+/g,'-')+'-listbox';const key=e=>{if(['ArrowDown','Enter',' '].includes(e.key)){e.preventDefault();setOpen(true)}else if(e.key==='Escape')setOpen(false)};const choose=o=>{setSelected(o);setOpen(false)};return <div class="kumo-field"><label for={id()+'-trigger'} class={p.hideLabel?'sr-only':''}>{p.label}</label><button type="button" role="combobox" id={id()+'-trigger'} aria-controls={id()} aria-expanded={open()} aria-haspopup="listbox" aria-busy={p.loading||undefined} aria-invalid={p.error?true:undefined} disabled={p.disabled||p.loading} onClick={()=>setOpen(!open())} onKeyDown={key} class={'kumo-trigger '+(p.size||'base')}><span>{selected()||p.placeholder}</span><svg aria-hidden="true" width="16" height="16" viewBox="0 0 16 16"><path fill="currentColor" d="m4 6 4 4 4-4z"/></svg></button>{open()&&<ul id={id()} role="listbox">{["Apple","Banana","Cherry"].map(o=><li role="option" aria-selected={selected()===o} tabIndex="-1" onClick={()=>choose(o)}>{o}</li>)}</ul>}{p.error?<p class="error">{p.error}</p>:p.description?<p class="description">{p.description}</p>:null}</div>}
