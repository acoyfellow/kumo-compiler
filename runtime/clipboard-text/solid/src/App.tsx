import {createSignal} from 'solid-js';
export default function ClipboardText(){const [status,setStatus]=createSignal('');async function copy(){const value='npm i kumo';try{await navigator.clipboard.writeText(value)}catch{const input=document.querySelector<HTMLInputElement>('#copy-value')!;input.select();document.execCommand('copy')}setStatus('Copied npm i kumo')}return <main class="form-shell"><h1>ClipboardText</h1><section class="form-grid"><article class="form-card" data-member="clipboard-text"><div class="field"><label for="copy-value">Install command</label><span class="row"><input class="control" id="copy-value" readOnly value="npm i kumo"/><button class="action copy" type="button" onClick={copy} onKeyDown={e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();copy()}}}>Copy</button></span><p class="status" role="status" aria-live="polite">{status()}</p></div></article></section></main>}
