import {For,createSignal} from 'solid-js';const pages=[1,2,3];export default function Pagination(){const [page,setPage]=createSignal(1);return <main class="selection-shell"><h1>Pagination</h1><nav class="pagination" aria-label="Pagination"><button aria-label="Previous page" disabled={page()===1} onClick={()=>setPage(p=>Math.max(1,p-1))}>‹</button><For each={pages}>{p=><button class="page" aria-current={page()===p?'page':undefined} onClick={()=>setPage(p)}>{p}</button>}</For><button aria-label="Next page" disabled={page()===3} onClick={()=>setPage(p=>Math.min(3,p+1))}>›</button></nav></main>}
