import {createSignal} from 'solid-js';export default function DateRangePicker(){const [start,setStart]=createSignal('2026-06-01'),[end,setEnd]=createSignal('2026-06-20');return <main class="selection-shell"><h1>DateRangePicker</h1><label>Start date<input type="date" value={start()} onInput={e=>setStart(e.currentTarget.value)}/></label><label>End date<input type="date" min={start()} value={end()} onInput={e=>setEnd(e.currentTarget.value)}/></label><p role="status" aria-live="polite">{start()+' – '+end()}</p></main>}
