import {For,createSignal} from 'solid-js';
const labels=["File","Edit","View"] as const;
export default function MenuBar(){const [active,setActive]=createSignal(0);let items:HTMLButtonElement[]=[];const move=(event:KeyboardEvent,index:number)=>{let next=index;if(event.key==='ArrowRight')next=(index+1)%labels.length;else if(event.key==='ArrowLeft')next=(index-1+labels.length)%labels.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=labels.length-1;else return;event.preventDefault();setActive(next);items[next]?.focus()};return <main class="nav-shell"><h1>MenuBar</h1><div class="menubar" role="menubar" aria-label="Application"><For each={labels}>{(label,index)=><button ref={el=>items[index()]=el} role="menuitem" tabindex={active()===index()?0:-1} onClick={()=>{setActive(index());items[index()]?.focus()}} onKeyDown={event=>move(event,index())}>{label}</button>}</For></div></main>}
