import {createSignal} from 'solid-js';
export default function Input(){const [value,setValue]=createSignal(""),[touched,setTouched]=createSignal(false);const invalid=()=>touched()&&!value().trim();return <main class="form-shell"><h1>Input</h1><section class="form-grid"><article class="form-card" data-member="input"><label class="field" for="email"><span>Email address</span><input class="control" id="email" type="email" required placeholder="you@example.com" value={value()} aria-describedby="email-error" aria-invalid={invalid()} onInput={e=>setValue(e.currentTarget.value)} onBlur={()=>setTouched(true)}/><small id="email-error" role="alert">{invalid()?'Email is required.':''}</small></label></article></section></main>}
