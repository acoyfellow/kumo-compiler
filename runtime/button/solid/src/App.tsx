import {For,Show,createSignal,splitProps,type JSX} from 'solid-js';
export type ButtonProps=JSX.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'outline'|'danger'|'ghost';size?:'xs'|'sm'|'base'|'lg';loading?:boolean;leadingIcon?:JSX.Element;trailingIcon?:JSX.Element};
export function Button(props:ButtonProps){const [local,rest]=splitProps(props,['variant','size','loading','leadingIcon','trailingIcon','children','disabled','class']);return <button {...rest} type={rest.type??'button'} class={`kumo-button ${local.variant??'primary'} ${local.size??'base'} ${local.class??''}`} disabled={local.disabled||local.loading} aria-busy={local.loading||undefined} aria-disabled={local.disabled||local.loading||undefined}><Show when={local.loading} fallback={<>{local.leadingIcon}{local.children}{local.trailingIcon}</>}><span class="spinner" aria-hidden="true"/> Loading</Show></button>}
const variants=["primary","secondary","outline","danger","ghost"] as const,sizes=["xs","sm","base","lg"] as const;export default function ButtonDemo(){const [count,setCount]=createSignal(0);return <main class="form-shell"><h1>Button</h1><section class="button-matrix"><For each={variants}>{v=><Button variant={v} onClick={()=>setCount(x=>x+1)} leadingIcon={<span aria-hidden="true">+</span>}>{v}</Button>}</For><For each={sizes}>{s=><Button size={s}>{s}</Button>}</For><Button loading>Save</Button><Button disabled>Disabled</Button><Button aria-label="Icon action">★</Button><p role="status">Activations: {count()}</p></section></main>}
